import Tests.Reflections.ProducerGuardedTrap
import Tests.Reflections.VerifyDeferredTaskLandedThenFold
import Tests.Reflections.CycleBreaksAtBase
import Tests.Reflections.ConverseForwardAsymmetry
import Tests.Reflections.ReductionByImport
import Tests.Reflections.ParametricAssemblerExtraction
import Tests.Reflections.ConvergentReduction
import Tests.Reflections.RecursiveDeliverableProjectToFlat
import Tests.Reflections.ConsumerJointBeforeProducer
import Tests.Reflections.AdditiveParallelType
import Tests.Reflections.UniversalPackagingJoint
import Tests.Reflections.ProjectionFamilyCompletion
import Tests.Reflections.ProducerDualOfConsumer
import Tests.Reflections.EmitProducerStrengthening
import Tests.Reflections.StoredVsProjectedRecursionEdge
import Tests.Reflections.FreeProjectionInvariant
import Tests.Reflections.BoundaryBoundRecovery
import Tests.Reflections.FoldConsumerChain
import Tests.Reflections.UniversalProducerRootSeed
import Tests.Reflections.DescendStepConsumerJoint
import Tests.Reflections.StructuralMovesAdvance
import Tests.Reflections.EntryBoundaryLocator
import Tests.Reflections.WidthRecursionCombinator
import Tests.Reflections.GuardThreadingSkeleton
import Tests.Reflections.GuardEdgeFloorAsymmetry
import Tests.Reflections.AdditiveParallelGuard
import Tests.Reflections.DescendStrengthInvariant
import Tests.Reflections.LiftOnceMirrorEasyEdge
import Tests.Reflections.ProducerGuardedOracleReconstruct
import Tests.Reflections.NearLeafMirrorShedsMachinery
import Tests.Reflections.ThreadedGuardPrunesCaseTree
import Tests.Reflections.IncompleteProjectionStillFactors
import Tests.Reflections.NonRestrictionResidualNeedsRootSeed
import Tests.Reflections.MinimalPairExtractsTheGate
import Tests.Reflections.WindowAbsoluteGateSubsetRestriction
import Tests.Reflections.BoundaryResidualEndDual
import Tests.Reflections.PrefixGateReconstructedFromBoundary
import Tests.Reflections.GuardedUniversalFoldRelocatesGuard
import Tests.Reflections.RootSeedRecursiveProducerSwap
import Tests.Reflections.ProbeDeferredProviderHeadBlindGate
import Tests.Reflections.RebaseFactFromEnclosingWindow
import Tests.Reflections.ProbeProviderSatisfiableBeforeAssembler
import Tests.Reflections.CoerceToWeakerReuseWrapper
import Tests.Reflections.RootSeedDiscriminatorNotFromGate
import Tests.Reflections.FromLocatedAssemblerDirection
import Tests.Reflections.BackwardLocatorMirrorsForward
import Tests.Reflections.MetricBridgeIsComposition
import Tests.Reflections.DownstreamDeriskRestoresUpstream
import Tests.Reflections.TypeBlindInvariantTransportsViaConverseFrame
import Tests.Reflections.FloorBlindGateAdmitsCrossSibling
import Tests.Reflections.TwoFloorRelayCloseBound
import Tests.Reflections.ImportForcedParametricGuard
import Tests.Reflections.VacuousPositiveCannotExclude
import Tests.Reflections.NarrowFromRootBreaksRederivationCycle
import Tests.Reflections.PushBlindFramePreserveDependent
import Tests.Reflections.ComplementGuardNotSufficient
import Tests.Reflections.AdvanceArmBalanceFree
import Tests.Reflections.SeveredEdgeBoundsNavigator
import Tests.Reflections.AggregateCollapsesStructuredSeparates
import Tests.Reflections.DomainRestrictionPreservationNegation
import Tests.Reflections.ProducerStopsAtSeveredEdge
import Tests.Reflections.ProbeRefutesComplementProvider
import Tests.Reflections.EndFreeGateUnderdeterminesClose
import Tests.Reflections.ContextFreeAdapterKnot
import Tests.Reflections.FieldAuditLocalizesKnot
import Tests.Reflections.SeparatorBlindTypedInvariant
import Tests.Reflections.CarrierProvenanceNotType
import Tests.Reflections.ParentRelativeGConjunct
import Tests.Reflections.ProjectionUpStrengthOrder
import Tests.Reflections.DescopeIsRouteRelative
import Tests.Reflections.NavigatorDispatchArithmetic
import Tests.Reflections.LeafProductionIsPrefixSlice
import Tests.Reflections.ReadAheadConsumerProofState
import Tests.Reflections.WrapperCarriesInvariant
import Tests.Reflections.VestigialProjectionReopens
import Tests.Reflections.NavigatorArmsKeyedToPositions
import Tests.Reflections.CarriedInvariantAdvanceEdge
import Tests.Reflections.ResidualAlreadyProvenInSibling
import Tests.Reflections.ComposeArmSeamBeforeSkeleton
import Tests.Reflections.RefuteArmMirrorsPinningSibling
import Tests.Reflections.TypeBlindBridgeFalse
import Tests.Reflections.DeferredTypedFactInGuard
import Tests.Reflections.DemotedDispatchMetricFromStructure
import Tests.Reflections.AdjacentOriginReconstructs
import Tests.Reflections.GuardStructureArmByArm
import Tests.Reflections.StrictContainmentNeedsOpener
import Tests.Reflections.AdditiveFieldCostByKeying
import Tests.Reflections.SlicePositionalBridgeGetElemQ
import Tests.Reflections.HeadBlindCoreServesBothArms
import Tests.Reflections.TargetAnchoredTwinRefutesWalkBreak
import Tests.Reflections.DeltaGenericBoundaryFamily
import Tests.Reflections.RebracketReusesPrefixBridge
import Tests.Reflections.MetricMinimumCollapsesDispatch
import Tests.Reflections.SaturationCleavesTerminalBranch
import Tests.Reflections.DispatchRoleFactorsTypeMetric
import Tests.Reflections.CarveLeavesToEliminatorOutput
import Tests.Reflections.RootSeedDerivesShapeDefersValue
import Tests.Reflections.DeferredStructuralAlreadyProvenBySibling
import Tests.Reflections.ConjunctiveConsumerGatesOnOrthogonalAxis
import Tests.Reflections.LocateConsumerByGateStrength
import Tests.Reflections.WholeDomainSiblingViaWeakerGuard
import Tests.Reflections.DepthGateRestrictionDiagnostic
import Tests.Reflections.DeepConjunctRootSeedOnly
import Tests.Reflections.RestrictionHidesRootFalsity
import Tests.Reflections.RescopeByExcludingPremise
import Tests.Reflections.AllDepthOverreachSourceGlobally
import Tests.Reflections.RedirectedProviderContractFirst

/-!
# Reflections — runnable proof-engineering demonstrations

This library collects the self-contained (core Lean, no `L4YAML` import) runnable
illustrations of the proof-engineering principles documented as numbered *Reflections* in
`Blueprint/08-initiative-4-intrinsic-foundations.md`.  They are kept separate from the
behavioural L4YAML test suites (parsing/emitting/round-trip) because they assert *methodology*,
not library behaviour: each is a toy model whose `#guard`/`#guard_msgs`/`#eval`/`by decide`
checks fail the build if the principle's positive/negative witnesses ever drift.

Each module names the Blueprint Reflection it illustrates:
* `ComplementGuardNotSufficient`          — Reflection 333 (the COMPLEMENT of a sufficient dispatch guard need not be a sufficient guard — the toy of `recseqbody_descend`, the DESCEND arm. A spine-walk splits into ADVANCE (`p` past the head entry) and DESCEND (`p` inside it). The ADVANCE guard `bal lo p = 0 ∧ lo < p` is SUFFICIENT for "past the head"; the NAIVE negation `bal lo p ≥ 1` is NOT sufficient for "inside the head" — `p` may be nested in a LATER entry (floor-blind, [[ref-floor-blind-gate-admits-cross-sibling]] on the complement side). The real DESCEND guard is the window-absolute FLOOR `∀ i ∈ (lo,p], bal lo i ≥ 1`. Witness `1 , [ [ 2 ] ]` (interior of `[1,[[2]]]`): a SCALAR head `1` (span `[0,1)`), then a doubly-nested seq. NEGATIVE (`#guard`): `bal 0 3 ≥ 1` holds so the negated guard fires, yet `p = 3 ≥ headEnd = 1` (past the head) and the FLOOR fails — `bal 0 1 = 0` (the head's return-to-`0`); the floor is the discriminator the bare `≥ 1` lacks. NEGATIVE (`#guard`): `delta op = delta mop` — a seq opener and a map opener share delta `+1`, so head-is-`.seq` is provably NOT a balance fact and enters as the dispatch hypothesis `h_lo_open`, not a derivation. POSITIVE (proven `floor_implies_in_head`, the crux of `recseqbody_descend`'s `h_p_lt`): abstractly over any `B`, if the head balances to `0` at its end `m` (`B m = 0`, `lo < m`) and the floor holds over `(lo,p]`, then `p < m` — the floor at `i = m` would force `B m ≥ 1`, contra `= 0`. The guard/complement analogue of [[ref-converse-forward-invariant-asymmetry]]; applies [[ref-window-absolute-gate-subset-restriction]] / [[ref-minimal-pair-extracts-the-gate]])
* `AdvanceArmBalanceFree`                  — Reflection 334 (the GENERAL ADVANCE arm is BALANCE-FREE once the caller supplies the decomposition — the toy of `recseqbody_advance_general`. R332's `recseqbody_advance` guarded advancing past the head with a BALANCE fact `bal lo p = 0` and spent its bulk USING that balance to RECONSTRUCT the structural `p ≥ lo + e.length`. R334 finding: the balance was never intrinsic to advancing — the CALLER already extracts the head entry and dispatches on `p ≥ lo + e.length`, so it already holds that inequality AND the `cons` decomposition `body = head ++ sep :: rest`; pass them DOWN and the balance machinery EVAPORATES, leaving pure slice algebra + one opener-vs-separator inequality. Witness `1 , [ [ 2 ] ]`: SCALAR head `1` (`headEnd = 1`), then a doubly-nested seq. NEGATIVE (`#guard`): `bal 0 3 = 1 ≠ 0` so the balance route cannot advance at the nested `p = 3`, yet `p ≥ headEnd` (the structural route can); the separator/opener tokens differ (`tok headEnd = .comma ≠ .op = tok 3`) so `p ≠ headEnd` is a CONSTRUCTOR inequality, not a balance fact. POSITIVE (proven `advance_rebase`): GIVEN `head ++ sep :: rest`, the re-base `(head ++ sep :: rest).drop (headLen + 1) = rest` is pure `List.drop_append` — no balance, no floor. POSITIVE (proven `sep_ne_opener`): if `body[headLen]? = some sep`, `body[p]? = some opener`, `sep ≠ opener`, then `p ≠ headLen`. The upstream dual of [[ref-reconstruct-in-place-over-relocate]]; resolution pattern for [[ref-complement-guard-not-sufficient]]; an [[ref-additive-parallel-type-over-shared-edit]] sibling)
* `SeveredEdgeBoundsNavigator`           — Reflection 335 (a SEVERED recursion edge bounds a navigator's domain; a PATH-blind gate admits windows past it — the toy of the `SeqMapPathNestedProbe` de-risk that REFUTED `rec_seq_body_nested_project`'s totality. A producer navigating a recursive deliverable can only reach windows whose PATH lies within the type's STORED edges; a member that PROJECTS rather than STORES the sub-deliverable (`RecSeqEntry.map` carries `WellBracketed`, not `RecSeqBody`) SEVERS the navigation. The consumer gate is PATH-BLIND (pins the IMMEDIATE enclosure, not the path), so it admits a map-PATH-nested seq window the navigator can't inhabit — invisible to a probe that only descends the navigable case; probe the PATH. Toy `Body`: `.seq` STORES the inner `Body`, `.map` PROJECTS to a weaker `Flat`. NEGATIVE (`#guard`): `bodyInner? (.map (.wrap .leaf)) = none` (navigator dies at the severed edge) though `flatIsSeqHead (.wrap .leaf) = true` (gate passes); `map_never_navigable`/`seq_always_navigable` show the severed edge is intrinsic to the constructor. POSITIVE (proven `flatProvider`): serve the gate-passing target from the WEAKER `Flat` substrate — the fix is RESTRICT the navigator + a flat provider, NOT strengthen the type. Sharpens [[ref-stored-vs-projected-severs-recursion-edge]] + [[ref-probe-provider-head-blind-gate]]; totality-refuting variant of [[ref-downstream-derisk-restores-upstream]])
* `AggregateCollapsesStructuredSeparates` — Reflection 336 (the dispatch is the un-aggregated STRUCTURED STATE, not a numeric aggregate of it — the toy of `SeqPathDispatchProbe`. After a [[ref-severed-edge-bounds-navigator-domain]] de-risk refutes a navigator's totality, find the DISPATCH partitioning the path-blind gate's windows into navigable vs unreachable by probing a numeric AGGREGATE against the un-aggregated STRUCTURED STATE it was folded from, over a minimal pair. Toy: a typed-frame `Stack`; `depth` is the `Nat` aggregate (the `flowBracketBalance` analogue), `pathAllSeq` reads the whole stack (the `btFold` analogue), `topFrame` is the gate's path-blind projection. NEGATIVE (`#guard` + `aggregate_collapses`): the map-path stack `[true,false,true]` and all-seq `[true,true,true]` have EQUAL `depth` 3 and EQUAL `topFrame` `some true` — the aggregate COLLAPSES the pair (REJECTED; the collapse IS the necessity leg). POSITIVE (`#guard` + `example`): `pathAllSeq` is `false` vs `true` — the buried map frame SEPARATES them; the structured state read WHOLE is the dispatch, the gate's `topFrame` only its head-projection. Applies [[ref-minimal-pair-extracts-the-gate]] to a PATH-dispatch; discharges the reshaped next step of [[ref-severed-edge-bounds-navigator-domain]])
* `DomainRestrictionPreservationNegation` — Reflection 337 (de-risk a navigator's DOMAIN-RESTRICTION with a PRESERVATION/NEGATION pair — the toy of `SeqPathAllSeq`'s descend lemmas. When a severed edge bounds a navigator's domain ([[ref-severed-edge-bounds-navigator-domain]]) and you RESTRICT the domain to fence it out, prove BOTH that the KEPT edge maps the domain into itself AND that the EXCLUDED edge maps it OUT — it is the NEGATION that licenses dropping the excluded recursion arm, not the preservation alone. Toy: a typed-frame `Stack`, `InDomain` = nonempty all-`true` (the `SeqPathAllSeq` analogue), `pushSeq`/`pushMap` the two edges. POSITIVE (proven `preservation`): `pushSeq` keeps an all-`true` stack in domain (the seq head discharges the restriction — `seqPathAllSeq_descend`). NEGATIVE (proven `negation`): `pushMap` BREAKS it (a map head is out-of-domain ⇒ no fifth map-mirror arm — `seqPathAllSeq_map_push_breaks`). `dominance`: the domain implies TOP `true` (`seqEnclosed_of_seqPathAllSeq`). `#guard`s pin the real minimal pair's stacks `[true,false,true]` (out) vs `[true,true,true]` (in). Lifts [[ref-converse-forward-invariant-asymmetry]] / [[ref-push-blind-frame-dependent]] from the TOP to the whole structured state [[ref-aggregate-collapses-structured-separates]])
* `ProducerStopsAtSeveredEdge` — Reflection 338 (a PRODUCER stops at a severed edge as a leaf; a NAVIGATOR dies there — the toy behind `rec_seq_body_nested_project`'s one-line landing. When a severed/projecting edge makes a NAVIGATOR non-total ([[ref-severed-edge-bounds-navigator-domain]]), check whether the deliverable can be PRODUCED instead of navigated before restricting the domain. Toy: a `Carrier` tree whose `box` member stores only a flat `Bool` (no recursive child — the `RecSeqEntry.map` analogue). NEGATIVE (proven `severed_navigation_stuck`): `navigate` DIES at a `box` window — it must descend INTO the member and there is no child (R335). POSITIVE (proven `severed_production_total`): `produce` STOPS AT the box, emits the leaf, recurses no further — TOTAL where navigation dies (`seqWindowRecSeqBody`'s near-leaf map oracle). `kept_edge_agrees`: the two agree on the KEPT seq edge, diverging only at the severed one. `#guard`s pin the divergence: a box-buried window navigates to `none` but produces `.box true`. For a `Prop` deliverable the two are interchangeable (proof-irrelevance) ⇒ prefer production, it sidesteps the edge; the domain restriction survives only as the consumer's routing tag ([[ref-coerce-to-weaker-reuse-wrapper]]), the four navigate-around arms an alternative not a necessity)
* `ProbeRefutesComplementProvider` — Reflection 339 (a probe of a would-be COMPLEMENT provider REFUTES its necessity — the toy behind `seqEnclosed_of_seqTypedInterior`'s one-line landing. When a navigator framing demands a domain PARTITION + a second provider for the excluded windows, PROBE before authoring: is the producer even sensitive to the partitioning predicate, is its real need PATH-INVARIANT, and does the consumer's GATE definitionally contain it. Toy: a `Stack` of typed frames; `Enclosed` = the HEAD is a seq (path-invariant); `StrongPath` = the WHOLE stack all-seq; `Gate` whose `enc` field IS `Enclosed`. BRIDGE (proven `enclosed_of_gate := g.enc`): the gate supplies the producer's need in one line ([[ref-conjunct-of-projection-is-free-field]] at the gate). PROBE (proven `probe_not_strongPath` + `probeGate`): the window `[true, false]` FAILS `StrongPath` (a `false` deeper down) yet the `Gate` HOLDS — so the producer serves the `¬StrongPath` window through the gate, NO complement needed. `#guard`s pin it: `probeStack.all (·==true) = false` but `probeStack.head? = some true`. So `StrongPath` is VESTIGIAL and ONE producer serves the whole gated domain — the size-down dual of [[ref-downstream-derisk-restores-upstream]] (the discriminator was never needed; the gate carried it))
* `EndFreeGateUnderdeterminesClose` — Reflection 340 (an END-FREE gate underdetermines a window's CLOSE — the toy behind the `(i'-b-B2c-provider-from-gate)` probe `SeqGateWindownessProbe`. A provider quantified under a gate whose conjuncts constrain only window-INTERNAL invariants (balance, enclosure mark, Dyck floor — none mentioning the token AT the end `b`) cannot read its structural close off `get s b`. Toy: `Tok := op | cl | it | sep`; `bal`/`windowBal` over a slice; an END-free `Gate` with one balance-`0` conjunct. PROBE (proven `gate_24` + `end_24_not_close`): the `b ≠ close` window `[2,4)` = `it sep` (mirror of `"1" ,`) on `W = op op it sep it cl sep it cl` (mirror of `[[1,2],9]`) PASSES the gate yet `get W 4 = it`, NOT `cl`. BRIDGE (proven `located_close_24`): the close is read off the LOCATED enclosing structure `hiS = 5` (`get W 5 = cl`), the window SELECTED inside by `4 ≤ 5` — never off `b`, mirroring `seqDescent_provider_of_located`. `#guard`s pin it: `windowBal W 2 4 = 0` but `get W 4 ≠ cl`, while `get W 5 = cl`. END-dual of [[ref-probe-provider-head-blind-gate]] (head-blind admits spurious-HEADED windows; end-free admits spurious-ENDED ones); the existing satisfiability probe MASKED it by only using `b = close` windows. SHARPENED by Reflection 356 (`gate_admits_three_ends`/`close_token_separates_three_ends` on `V = op it sep it sep it cl`): a SETTLED next-step pointer (the emission-spine wrapper `nestedSeq_recseqentry_locate`, R355) re-credited balance-0 with pinning `b`; re-running the probe shows the gate admits not one but ARBITRARILY MANY non-close ends (a seq of `n` elements has `n-1` separator ends, all balance-0 + floored), so `b` is pinned by the consumer's explicit close hypothesis `tokens[hi]!.val = .flowSequenceEnd` — `seqWindowRecSeqBody`'s fourth `G`-conjunct — not by the interior gate; re-probe a boundary-pinning attribution even when a settled pointer states it)
* `ContextFreeAdapterKnot`                 — Reflection 343 (a CONTEXT-FREE IH adapter cannot thread a CONTEXT-DEPENDENT carrier fact — the toy behind the `(i'-b-B2c-desc-fixpoint-assembly)` probe. To break the carrier↔recursion circularity, the carrier-free `seqWindowRecSeqBody` would thread `FlowBodyContent` as a recursion `G`-conjunct instead of looking it up from the ambient carrier. The probe asked: clean ordering or co-recursive knot? KNOT, two ways. Toy: opaque `Rec`/`Content : Nat → Prop` (toy `RecSeqBody`/`FlowBodyContent` keyed on window width); `content_of_rec : ∀ w, Rec w → Content w` (the SAME-window edge, toy of `SafeBodyUnit`-of-`RecSeqBody` — R296); `dispatch : ∀ w, Content w → (∀ w' < w, Rec w') → Rec w` (the dispatch needs the CURRENT window's `Content` + a CONTEXT-FREE `Rec`-IH, toy `recseqentry_window_dispatch`). POSITIVE (`rec_via_carrier`): the GLOBAL carrier `∀ w, Content w` closes the producer — it answers `Content` at every window WITHOUT recursion. NEGATIVE (`rec_guarded_needs_carrier` + `content_same_window`): threading `Content` through the guard buys NOTHING — the context-free IH adapter `fun w' hw' => ih w' hw' ?` must inject `Content w'` for ARBITRARY smaller `w'`, and the only carrier-free source `content_of_rec` needs `Rec w'` (the output), so the step must take the whole carrier as an undischarged hypothesis (`Content w ⟵ Rec w ⟵ dispatch ⟵ Content w`, a SAME-window cycle width recursion cannot break; `P := Rec ∧ Content` does not untie it). RESOLUTION (`rec_resolved` + `content_resolved`): the knot cuts only by a `Content`-FREE dispatch (reconstruct its three head/first-entry facts carrier-free, [[ref-prefix-gate-reconstructed-from-boundary]]); then `Rec` closes with NO carrier and `Content` is a downstream COROLLARY. `#guard`s smoke-test the resolved + carrier routes on the trivial model. Why [[ref-cycle-breaks-at-base]]'s base-swap does NOT apply here (the cycle is per-window, not just at the root); the carrier was the external oracle [[ref-narrow-from-root-breaks-rederivation-cycle]] relied on)
* `FieldAuditLocalizesKnot`               — Reflection 344 (a multi-field guard INPUT localizes its dependency knot to ONE load-bearing field — the AUDIT behind the `(i'-b-B2c-dispatch-defront)` de-front, the move after R343's knot. The pivot removes `FlowBodyContent` from `recseqentry_window_dispatch`'s input, but the input is a THREE-field guard; the naïve fear is the WHOLE guard creates the cycle. The audit shows otherwise: `headContentStart` is a FREE projection (definitionally the SAME proposition as `FlowBodyContentDeep.headContentStart`, which the recursion already carries); `feContentStart` is DEAD (consumed by NEITHER the dispatch nor either bracket oracle — it serves the advance-tail plumbing, a sibling consumer); `bodySucc` is the GENUINE residual (R296: no all-depth balance-free form, NOT a field of `FlowBodyContentDeep`). So the knot localizes to the SINGLE field `bodySucc` at the SINGLE position the first-entry END. Toy: `Deep`/`Guard` structures with shared `head` target; `dispatch_via_guard` (carrier route, touches only `head`+`succ`). POSITIVE (`dispatch_defront` + `dispatch_via_deep_and_succ`): the consumer is inhabitable from the carried `Deep` (free `head`) + the lone residual `succ`, with NO `feStart` field in its signature; the full-`Guard` route factors through it. NEGATIVE (`deep_holds_without_succ` + `defront_needs_succ`): `succ` is irreducible — `Deep True True` holds while `Succ := False`, so no `Deep → succ` exists; the de-front must take `h_succ` standing. The free + dead fields MASKED which one field is load-bearing; auditing field-by-field shrinks the producer's debt to its minimum. Sharpens [[ref-incomplete-projection-still-factors]] (per-field granularity) + [[ref-conjunct-of-projection-is-free-field]] (the free-field mechanism); localizes [[ref-context-free-adapter-knot]])
* `SeparatorBlindTypedInvariant`         — Reflection 345 (a bracket-typed invariant is SEPARATOR-BLIND, so a separator-placement fact is provably not a projection of it — the PROBE behind `(i'-b-B2c-bodySucc-first-entry)`, R344's lone residual. The de-front works only if `FlowBodyContent.bodySucc`-at-first-entry-end is reconstructible CARRIER-FREE from the carried guards (`FlowBodyWindow.wellTyped` = the typed-bracket invariant `WellTyped`, and `FlowBodyContentDeep`). PROBE: REFUTED at the substrate level. The decisive mechanism is mechanical — `WellTyped`'s per-token step `btStep` matches the separator `.flowEntry` under the `_ => some s` IDENTITY catch-all (`WellBracketed.lean:906`), the SAME branch a content scalar takes, so the fold is utterly insensitive to separator placement. A MINIMAL PAIR settles it: two adjacent content units WITHOUT vs WITH a separator have the SAME `WellTyped` value, yet differ on `bodySucc`-at-first-entry; `FlowBodyContentDeep` is VACUOUS on a separator-free window (no openers/separators), so the full carried-guard set holds on the unseparated member while `bodySucc` fails. Hence NO `WellTyped l → bodySucc l` can exist. Toy: `Tok` (content/sep/opn/cls); separator-blind `step`/`fold`/`WT`; `firstSucc` (toy `bodySucc` at `k=0`); minimal pair `noSep`/`withSep`. POSITIVE (`wt_blind` + `firstSucc_sep`): both lists are `WT` — the invariant cannot tell them apart — and the SEPARATED one has the successor fact. NEGATIVE (`no_reconstruction` + `firstSucc_nosep` + `minimal_pair`): the UNSEPARATED list is `WT` yet fails `firstSucc`, so `¬ ∀ l, WT l → firstSucc l = true`; that non-existence is the substrate-level refutation — `bodySucc` is genuine EMISSION information, needing `SafeBodyUnit`. Sharpens R296 (no balance-free form → no typed-bracket-free form, via the explicit separator-blindness) + [[ref-type-blind-invariant-transports-via-converse-frame]] (separator-placement is the specific fact a type-blind invariant cannot CREATE); resolves the "if none" branch of [[ref-context-free-adapter-knot]] / [[ref-field-audit-localizes-knot]])
* `CarrierProvenanceNotType`             — Reflection 346 (a "narrow the root carrier" cycle-break works only if the carrier has an emission-INDEPENDENT producer; narrowability is provenance-blind — the PROBE behind `(i'-b-B2c-carrier-narrow)`, R345's resolution route. R345 charted the seq fixpoint to "source per-window `FlowBodyContent` from the EMISSION carrier `h_root_carrier`, narrowed to each window." PROBE: REFUTED as a TYPE MISIDENTIFICATION. A flat universal-over-sub-windows `Carrier lo hi := ∀ a, lo ≤ a → a ≤ hi → Fact a` is narrowable BY CONSTRUCTION (2-line `Nat.le_trans`), and that narrow lemma is PROVENANCE-BLIND — its proof depends only on the carrier's shape, so the SAME proof serves an emission-sourced carrier and a recursion-produced one ⇒ narrowability certifies NOTHING about circularity. The cycle-break works ONLY if the ROOT instance has a producer INDEPENDENT of the consuming recursion. But `h_root_carrier : SeqInteriorSeparators tokens 2 (size-2)` is NOT emission: its only producer `seqRoot_seqInteriorSeparators` consumes `desc`, whose nested half is the fixpoint `seqWindowRecSeqBody` that consumes `h_root_carrier` — the circle `carrier ⟸ desc ⟸ fixpoint ⟸ carrier`. Narrowing it relocates the circle, never breaks it; and the call site carries only the weaker `EmitScansInFlowBlock`, so emission's `RecSeqBody` is unreachable. Escape (already landed but unused): `seqSeparatorFacts_of_recseqbody` reads the facts off the window's OWN IH `RecSeqBody` (proof-irrelevant `Prop` = as good as a carrier lookup), seeded from emission by `emitList_scans_recseqbody`. Toy: flat `Carrier`; provenance-blind `narrow`; `Rec`/`carrier_of_rec` (IH route); provenance A `rec_from_emission` (recursion-independent) vs provenance B `Fix`/`Desc` (carrier-loop arrows). POSITIVE (`carrier_via_rec`): a recursion-independent producer yields the carrier UNCONDITIONALLY — cycle broken. NEGATIVE (`carrier_route_is_a_loop` + `narrow_same`): the carrier route composes to an ENDO-map `Carrier → Carrier` with no base, and `narrow` is the same endo shape — neither can seed. Sharpens [[ref-narrow-from-root-breaks-rederivation-cycle]] (needs an emission-INDEPENDENT root seed; a deliverable-typed "root carrier" is not one); carrier-side dual of [[ref-context-free-adapter-knot]])
* `ParentRelativeGConjunct`              — Reflection 347 (a PARENT-RELATIVE recursion-guard conjunct rides the recursion's structural SITES, not the uniform IH adapter — the PROBE that settled the live route for `(i'-b-B2c-ih-recseqbody-step)`, R346's handoff. R346 left two candidate carrier-free routes for the step's per-window `FlowBodyContent`: (A) read its separator facts off the window's OWN recursive output (`seqSeparatorFacts_of_recseqbody`), or (B) thread `FlowBodyContent` as a fifth `G`-conjunct propagated by the LANDED edges `seqRoot_flowBodyContent` (root, emission-seeded) / `flowBodyContent_descend` (646) / `flowBodyContent_advance` (4205). PROBE picks (B). FINDING: `FlowBodyContent`'s descend edge CONSUMES THE PARENT–CHILD RELATION (it takes the parent's `h_content` + the opener/close/floor), so the conjunct CANNOT be re-established in the dispatch's UNIFORM IH adapter (`recseqentry_window_dispatch`:5618/5629, which sees only the child's INTRINSIC four facts `FlowBodyWindow ∧ FlowBodyContentDeep ∧ SeqEnclosed ∧ close`, never how the child descends) — this is the R343 [[ref-context-free-adapter-knot]] knot, now RESOLVED: the four existing conjuncts are themselves supplied not in the adapter but at the recursion's STRUCTURAL CALL SITES (the seq oracle's descend, the assembler's advance), where the parent–child relation IS in scope; the adapter merely FORWARDS. So WIDEN the dispatch's IH SIGNATURE by the `F` conjunct and discharge it at those two sites via the landed edges. Route (A) is off the critical path: reading `F` off the window's OWN output is a same-window cycle for the producing window (output ≠ input) and redundant for children (the descend edge already builds it). The conjunct is NECESSARY: a nested window needs its own `bodySucc`, sourced only from the parent's `F` via descend — in scope only if `F` is threaded parent→child. Toy: `F` inductive (`root` no-parent | `descend` needs-parent); `Intrinsic` (the adapter's only data). POSITIVE (`F_root_seed` + `F_supply_at_descend`): the root seed and the structural-site supply both build `F` where the parent (or none) is in scope. NEGATIVE (`F_nonroot_carries_parent`): a nested `F` CARRIES a strictly-smaller parent (inversion), so no `Intrinsic lo hi → F lo hi` exists — the uniform adapter cannot manufacture it. NEGATIVE (`own_output_is_a_loop`): route (A) at the current window is an endo `F → F` with no seed, echoing R346's `carrier_route_is_a_loop`. RESOLVES the knot [[ref-context-free-adapter-knot]] diagnosed; corrects R346's framing — `seqSeparatorFacts_of_recseqbody` is off-path, the threaded `FlowBodyContent` edges are the route; applies [[ref-universal-producer-root-seed-first]] + [[ref-push-blind-frame-dependent]] (parent-dependent vs parent-blind edges))
* `ProjectionUpStrengthOrder`            — Reflection 348 (a carrier-free IH thread's DESCEND edge re-enters its OWN window: the five→four IH projection runs UP the strength order, and the combined deliverable RELOCATES the same-window cycle without breaking it — the PROBE that settled `(i'-b-B2c-dispatch-ih-widen)` step (3), run mechanically sorry-free in `Tests/Guards/Proofs/DescendIHProjectionProbe.lean` against the real defs. R347's route widens the dispatch IH by `FlowBodyContent` (`F`) and discharges it at the descend/advance sites; step (3) asks whether `flowBodyContent_descend`'s FOUR-conjunct `h_ih` projects from the widened FIVE-conjunct IH. FINDING 1 (STRICT STRENGTH ORDER): `IH4` (child `Out`/`RecSeqBody` from four INTRINSIC facts) is STRICTLY STRONGER than `IH5` (needs the child's `F` too) — four→five is a free drop (`ih5_of_ih4`), five→four closes IFF a per-window `provider : Intrinsic → F` exists (`ih4_of_ih5_iff_provider`), so the needed projection runs UP the strength order, impossible without a NEW producer for the dropped `F` — the carrier-free `F` the arc lacks (R296/R345). GENUINE arity entanglement, the fork's second branch. FINDING 2 (CYCLE SURVIVES COMBINATION): `F n` is read OFF `Out n` (`f_of_out`, toy `seqSeparatorFacts_of_recseqbody`) while `Out n`'s dispatch needs `F n` (`out_needs_f`); composed that is an endo with no seed (`same_window_loop`, R346's `carrier_route_is_a_loop` at ONE window), and the combined `Pair n := Out n ∧ F n` does not dissolve it (`combined_step_still_cyclic` — both fields trace through the same `F n` input) — combination relocates the cycle from the recursive edge INTO the step. THE BREAK (`break_via_emission`): only an EMISSION source of `F` independent of `Out` opens it — generalize the flat root seed (`seqRoot_safeBodyUnit`/`seqRoot_flowBodyContent`) to nested sub-values, making `F` a genuine four-conjunct-`G` INPUT. EDGE ASYMMETRY (the locus): `advance_supplies_child` is a clean parent→child arrow (NO `Out` in the loop, R347's optimistic shape — `flowBodyContent_advance` re-bases, no IH); `descend_supplies_child_via_out` routes the child's `F` through the child's own `Out` (`seqChild_safeBodyUnit`), which `out_needs_f` ties back — a same-window 2-cycle, because `bodySucc` has no all-depth balance-free form (R296) so descend cannot re-base down a level. Corrects R347's "landed edges" optimism (its toy abstracted descend as clean parent→child, hiding the `Out` re-entry); applies [[ref-push-blind-frame-dependent]] / [[ref-converse-forward-invariant-asymmetry]]; same-window loop is [[ref-carrier-provenance-not-type]]'s shape)
* `DescopeIsRouteRelative`               — Reflection 349 (a producer DESCOPED as "an alternative, not a necessity" UN-DESCOPES once the route that subsumed it is the one being eliminated; the emission seed EXTRACTS position-blind, it does not re-scan — the PROBE that settled `(i'-b-B2c-nested-fbc-emission-seed)`, with the offset gap `#guard`-confirmed in `Tests/Guards/Proofs/SeqNestedEmissionSeedProbe.lean`. R348 forced the per-window `FlowBodyContent` seed to be EMISSION-sourced per nested sub-value; the probe asks whether a NESTED seq window's `SafeBodyUnit` is sourceable from emission WITHOUT re-entering `seqWindowRecSeqBody`. FINDING 1 (POSITION-BLIND EXTRACTION): every `RecSeqBody`/`RecSeqEntry`/`SafeBodyUnit`/`ContentStartTok` constraint is `.val`-only, so the nested interior `RecSeqBody` STORED in the flat root `seqRoot_recseqbody`'s `RecSeqEntry.seq.h_rec` field sits over the EXACT outer-array slice (right `.pos`s) — the ASSEMBLE is LANDABLE NOW from an emission `RecSeqEntry` via `recseqentry_seq_extract` + `interior_window_eq` + `.toSafeBodyUnit` (`nestedSeq_safeBodyUnit_of_entry`, sorry-free, NO carrier/recursion). A standalone RE-SCAN is the WRONG object (`reScan_wrongObject`): same `.val`s but `.pos` offsets shifted by the enclosing `[` (`#guard`ed: in-context `2,5,7` vs standalone `1,4,6` on `[[1,2],9]`). FINDING 2 (DESCOPE IS ROUTE-RELATIVE): reaching the stored field at an arbitrary nested window is the R330–R337 SPINE-WALK navigator, DESCOPED as "an alternative, not a necessity" (`rec_seq_body_nested_project` is the carrier SHORTCUT — `seqWindowRecSeqBody` + `SeqEnclosed`-from-path, needs `h_root_carrier`, re-enters the recursion) BECAUSE the carrier route served the whole domain; but the carrier route is exactly what the emission seed ELIMINATES, so removing it UN-DESCOPES the navigator (`spineWalk_redundant_given_richRoute` the historical descope, conditional on `richRoute`; `seed_needs_spineWalk_without_carrier` / `descope_is_route_relative` the un-descope once `Carrier` is withheld). THE RULE: "X is redundant — R delivers it" is conditional on R staying in the design; a goal that eliminates R silently re-owes every X descoped because of R. The residual is the emission-spine-walk LOCATOR (`ref-from-located-assembler-direction` bottom-up; R330 already `#guard`-de-risked its measure + offsets). Sharpens [[ref-reduction-by-import]] / [[ref-downstream-derisk-restores-upstream]] (a later consumer re-opens a descoped PRODUCER, not a deliverable); extract is [[ref-coerce-to-weaker-reuse-wrapper]]'s val-transfer; offset gap is [[ref-probe-provider-satisfiable-before-assembler]]'s right-object check)
* `NavigatorDispatchArithmetic`          — Reflection 350 (a position-indexed navigator splits DECISION from CORRECTNESS: the bottom-up locator's branch dispatch is pure offset/length arithmetic, balance is demoted to the correctness side — the PROBE that settled `(i'-b-B2c-nested-fbc-emission-locator)`, `move_trichotomy`/`move_exclusive` pure `omega`, `#guard`ed on three witnesses in `Tests/Guards/Proofs/SeqNestedEntryLocateProbe.lean`. R349 left the emission-spine-walk LOCATOR as the residual; the probe asks its single-step shape — (a) one offset-tracked `body.length` recursion, or (b) a separate balance-locate front end. FINDING (option a): a navigator over a recursive deliverable splits into a DECISION procedure (which branch) and a CORRECTNESS justification (why); the decision is the cheap part. Walking `RecSeqBody body` with base offset `off`, the head entry `e` occupies `[off, off+e.length-1]`, so the target window start `a` selects the move by comparing `a` against `off+1` and `off+e.length` ALONE — leaf (`a=off+1`) / descend (`off+1<a<off+e.length`, off'=off+1) / advance (`off+e.length<a`, off'=off+e.length+1), `a=off+e.length` the impossible separator/close (`navMove`/`classifyMove` total, `classify_leaf`/`_descend`/`_advance`/`move_exhaustive`/`move_exclusive` pure `omega`). The structure STORES each entry's length and the offset-slice invariant `body=(take H).drop off` ties it to absolute coordinates, so the navigator NEVER calls balance to decide. BALANCE IS DEMOTED to correctness: R330's TOP-DOWN projection selected the entry by `flowBracketBalance=0` (it had a token coordinate, had to FIND the entry); this BOTTOM-UP locator keyed on `[a,b)` locates by offset arithmetic, balance entering only the PROOF that the slice equals the window (via all-seq-path/`WellTyped`), never the selector (`topDown_needs_balance` vs `bottomUp_decides_by_arithmetic`). THE RULE: before importing a locate's heavy front end into a navigator descending the same structure, ask which part is the DECISION vs the CORRECTNESS — if the deliverable stores the positional data the decision needs, the decision is a length-arithmetic trichotomy (`omega`) and the front end belongs only to correctness. Sharpens [[ref-backward-locator-mirrors-forward]] / [[ref-near-leaf-mirror-sheds-machinery]] (the backward/bottom-up direction sheds the forward locate's machinery) and [[ref-from-located-assembler-direction]] (the bottom-up single-step shape); the machinery was already paid for by the structure's stored lengths [[ref-metric-bridge-is-composition]])
* `LeafProductionIsPrefixSlice`          — Reflection 351 (the LEAF — the only deliverable-PRODUCING move of a position-navigator — is balance-FREE even on the CORRECTNESS side: its window identity is a pure prefix-slice fact; balance is confined to the non-producing interior MOVES. The AUTHORING result that landed the leaf of `nestedSeq_recseqentry_locate` (`(i'-b-B2c-nested-fbc-emission-locator-author)`), in `NonemptyStructure.lean` (`head_entry_slice` + `nestedSeq_recseqentry_locate_leaf`, sorry-free, verified-but-unconsumed). R350 split a navigator into DECISION (arithmetic) + CORRECTNESS (balance) and demoted balance to correctness; AUTHORING the leaf SHARPENS that — R350's "correctness needs balance" is too coarse. The leaf, the SINGLE move that produces the deliverable (descend/advance only re-base and recurse), has a correctness obligation — the window identity `(L.take (off+e.length)).drop off = e` — that is ITSELF balance-free: a pure `take`/`drop` prefix-slice fact derivable from the offset-slice invariant `(L.take H).drop off = e ++ rest` + fit bound `off+e.length ≤ H` via `take_take`/`drop_take`/`take_append_of_le_length` (`head_entry_slice`, PROBED axiom-clean before authoring). So at the leaf BOTH halves of R350's split are cheap: decision = length arithmetic (`leaf_decision_is_arithmetic`), correctness = prefix slicing (`leaf_correctness_balance_free`); importing balance is dead (`leaf_with_balance_redundant`). BALANCE IS CONFINED to the non-producing moves: only DESCEND/ADVANCE (which-child navigation) can use it, and they produce NOTHING — the producing move and the balance-using moves are DISJOINT (`producing_is_balance_free`: `produces m → ¬ usesBalance m`). "The producing case is the easy case." THE RULE: when a navigator stores children as prefixes/segments and carries an offset-slice invariant, AUTHOR THE LEAF FIRST and expect its correctness to be PURE SLICE ALGEBRA, not the metric the forward locate used; reaching for balance to justify the leaf's output is over-engineering. Sharpens [[ref-navigator-dispatch-is-arithmetic]] (R350: locates WHERE balance enters — not even the leaf's correctness, only the interior moves) and [[ref-near-leaf-mirror-sheds-machinery]] (the leaf sheds even the correctness-side metric); the slice fact is the window identity [[ref-from-located-assembler-direction]]'s assembler consumes)
* `ReadAheadConsumerProofState`          — Reflection 352 (read-ahead the CONSUMER chain to de-alarm a terse downstream step-list: proof-STATE, not prose, sets the pace. A trajectory de-risk that read steps (3)–(6) below the two `FlowSubrangesOk tokens := sorry` sites BEFORE grinding the mechanical locator bricks. A dependency map lists downstream steps as one-liners ("drive X … thread Y … wire Z"); a one-liner is INDISTINGUISHABLE between already-proven / trivial-plumbing / a whole parallel-effort — only the source's `theorem … := by` vs `:= sorry` tells them apart (`plan_prose_is_state_blind`: the plan exposes one constant bit across all states). A first-pass SURVEY flagged `EmitScansInFlowRecEntry` "definition only, no proof"; FALSE — `emit_scans_in_flow_rec_entry` (NonemptyStructure.lean:2418) proves it from `Grammable`. The survey checked the `def`, not the PRODUCER (`survey_overalarms_vs_source`, `source_verdict_reads_producer_not_def`: read producer not def). Net: no hidden deep obligation in (3)–(6); per-window facts carrier-supplied inside the proven `seqWindowRecSeqBody`, map RecMapBody step-lemmas landed, single open funnel = `seqRoot_seqInteriorSeparators`'s `desc` (which the `nestedSeq` locator builds). Verdict — not circling: the tree is bounded and SHALLOWER than the terse map read. THE PAYOFF is a PACING decision (`no_hidden_wall`): no wall behind the mechanical bricks ⇒ BATCH them instead of one-per-session. Grep for a PRODUCER (`: T := by`/`→ T`/`_of_`), not the `def T`; treat a multi-agent survey as a LEAD, source as the verdict. Complements [[ref-probe-deferred-universal-before-producing]] (probes a PRODUCER target; this probes a CONSUMER chain before pacing) and pairs with [[ref-leaf-production-is-prefix-slice]] (the bricks this cleared the runway for))
* `WrapperCarriesInvariant`              — Reflection 353 (the WRAPPER of a position-navigator is NOT among its mechanical slice bricks: partition the moves by what each PRODUCES, not by step-list position. The R352 de-risk batched "descend + advance + wrapper" as mechanical bricks; authoring descend + advance (`nestedSeq_recseqentry_locate_descend`/`_advance`, sorry-free) confirmed the SLICE half but exposed the wrapper's mis-binning. The leaf/descend/advance SLICE bricks re-base coordinates and produce NOTHING (`slice_bricks_produce_nothing`); the WRAPPER produces the located deliverable (`wrapper_produces_and_is_not_a_brick`) by threading a DOMAIN INVARIANT + a termination measure. THE TELL: its DESCEND arm (firing at `off+1 < a < off+entryLen`) excludes scalars/empties by ARITHMETIC alone (`scalar_empty_excluded_by_arithmetic` — empty descend range), but a MAP head SURVIVES the pure-arithmetic dispatch (`arithmetic_dispatch_admits_map` — `entryLen ≥ 4`) yet descending into a map interior is wrong. Arithmetic CANNOT separate a seq head from a map head (`arithmetic_cannot_separate_seq_from_map` — both survive); only the domain invariant `SeqPathAllSeq` does (`invariant_separates_seq_from_map`, via `seqPathAllSeq_map_push_breaks`). So the wrapper is the invariant-bearing assembly, needing its own interface-design probe before authoring — reaching for it as a "grind" after the slice bricks is the mis-binning. Sharpens [[ref-read-ahead-consumer-chain-proof-state]] (binning by step-LIST position mis-classifies the invariant-bearing assembly as a brick) and complements [[ref-leaf-production-is-prefix-slice]] (R351: the leaf produces nothing → slice algebra; the wrapper is its complement — produces everything, carries the invariant); interface discipline [[ref-from-located-assembler-direction]])
* `VestigialProjectionReopens`           — Reflection 354 (a VESTIGIAL projection re-opens for a structure-walking consumer: a whole-structure invariant retired as vestigial for a value-blind consumer — which reads only a TOP projection — is LOAD-BEARING for a sibling consumer that WALKS the typed structure. Vestigial-ness is CONSUMER-RELATIVE. The wrapper of `nestedSeq_recseqentry_locate` (R353) must carry a domain invariant; R354's PROBE (`SeqNestedWrapperIfaceProbe.lean`, `#guard`-green on real tokens) settles WHICH: the WHOLE-path `SeqPathAllSeq`, not the TOP `SeqEnclosed`/`SeqTypedInterior` the `desc` consumer hands it. MINIMAL PAIR — the SAME nested seq `[1,2]` reached two ways: `[[1,2]]` (all-`[` path, stack `[true,true]`) vs `[{x:[1,2]}]` (through a `{`, stack `[true,false,true]`). The negative's TOP is `true` (`pos_both`/`neg_top_holds_full_fails`) so it PASSES the projection, but its WHOLE path is not all-`true` so it FAILS the full invariant — `projection_equal_full_separates`: the projection canNOT separate the pair, the full invariant DOES. The value-blind consumer ADMITS the bad window (`value_blind_admits_bad_window`) the structure-walker must REJECT (`walker_rejects_bad_window`) — same invariant, opposite verdicts. And the bridge "projection ⟹ full" is FALSE (`projection_does_not_imply_full`, the negative is its counterexample), so the walker sources the full invariant from its OWN descent discipline (descends only through seq openers — R338), not from the projection-providing gate. Consumer-relative dual of [[ref-downstream-derisk-restores-upstream]]; the probe is [[ref-minimal-pair-extracts-the-gate]]; sharpens [[ref-wrapper-carries-invariant-not-a-slice-brick]] (names WHICH invariant and proves the weaker candidate insufficient); structured-vs-aggregate [[ref-aggregate-collapses-structured-separates]])
* `NavigatorArmsKeyedToPositions`        — Reflection 355 (a position-navigator's per-arm exclusion facts live at DIFFERENT positions: the DESCEND arm's DOMAIN at the recursion BASE `off`, the LEAF arm's head CLASSIFICATION at the TARGET WINDOW `a = off+1`. R354 settled the carried domain is the WHOLE-path `SeqPathAllSeq tokens off`; the blueprint then claimed the interface RESOLVED and attributed the LEAF arm's head-is-`.seq` to that SAME carried hypothesis. R355's PROBE (`SeqNestedLeafEnclosureProbe.lean`, `#guard`-green on real tokens) finds the attribution IMPRECISE and the carried-state list INCOMPLETE: `SeqPathAllSeq tokens off` is BLIND to `tokens[off]` (the head entry's own opener — it constrains only the stack BEFORE `off`), so it cannot decide whether the head is a `[` (seq, leaf admissible) or `{` (map, leaf inadmissible). Only the WINDOW's `SeqEnclosed tokens a` — the second conjunct of the `SeqTypedInterior tokens a b` that `desc` consumes — separates them. MINIMAL PAIR — SAME body base `off = 2`, SAME would-be leaf window `a = 3`, differing only in the head bracket TYPE: `[[1,2]]` (seq head, `SeqEnclosed 3 = true`) vs `[{x:[1,2]}]` (map head, `SeqEnclosed 3 = false`). They AGREE on the carried base domain (`SeqPathAllSeq tokens 2 = true` for BOTH — `carried_domain_blind_to_head`) but DISAGREE on the window enclosure (`seq_head_window_enclosed`/`map_head_window_not_enclosed`) — `base_equal_window_separates`: the carried base domain is EQUAL across the pair, the window enclosure SEPARATES it. `carried_domain_cannot_classify_head`: no function of the carried domain alone classifies the head, so the WRAPPER MUST ALSO CONSUME the target window's `SeqTypedInterior tokens a b` (the LEAF map-exclusion via `SeqEnclosed`, and the balance-0 that pins `b` to the head entry's edge). Sharpens R354's [[ref-vestigial-projection-reopens-for-structure-walker]] (R354 settled the BASE domain; R355 finds the LEAF arm needs a SECOND, window-keyed fact the base hypothesis cannot supply); the probe is [[ref-minimal-pair-extracts-the-gate]]; the per-arm position-keying is [[ref-two-boundary-consumer-joint]] for a navigator)
* `CarriedInvariantAdvanceEdge`           — Reflection 357 (a recursion that CARRIES a domain invariant needs one PRESERVATION edge per recursion MOVE — descend AND advance — not per deliverable constructor; the descend edge + its NEGATION mask the missing ADVANCE (frame) edge. Authoring the wrapper `nestedSeq_recseqentry_locate`, its ADVANCE arm must recurse with `SeqPathAllSeq tokens (off+e.length+1)`; the next-step pointer named "R337 seqPathAllSeq advance edge" as if it existed, but R337 authored only `seqPathAllSeq_descend` (the seq-head PUSH preserves) and `seqPathAllSeq_map_push_breaks` (the map-head PUSH breaks) — descend + negation, NO advance. That pair FEELS exhaustive (a [[ref-converse-forward-invariant-asymmetry]] pair: seq edge preserves, map edge cannot) but answers only the DESCEND axis; advance is a third, ORTHOGONAL obligation. CODE `seqPathAllSeq_advance` (SeqInteriorSeparators.lean, sorry-free, build green 614 jobs, frontier 4) is the whole-stack analogue of `seqEnclosed_advance`: a FRAME not a push — `WellTyped_frame` returns the fold to the SAME stack `s`, so all three conjuncts (defined/nonempty/all-`true`) transport VERBATIM, even more directly than the top-only `seqEnclosed_advance` which must re-read the head. The missing edge is the EASY one, which is exactly why the hard descend PUSH (and proving its negation) masked it. Toy `os`/`om`/`it`/`cl` bracket fold + `PathAllSeq` stack predicate: `descend_preserves` (PUSH true) / `advance_preserves` (FRAME a balanced segment) positives, `map_break` the negation; the `#guard`s make the masking literal — the advance frame is BLIND to the stack (`bfold [os,it,cl] [false] = [false]` preserves even a non-all-true stack) whereas a descend push CHANGES it (`bfold [os] [false] = [true,false]`), so descend + negation cannot supply the advance. Sharpens [[ref-structural-moves-complete-recursion]] (one move per DELIVERABLE constructor → here one preservation edge per recursion MOVE for the CARRIED invariant); a probe-before-producing win [[ref-probe-deferred-universal-before-producing]]; the residual is the leaf `b`-pin [[ref-end-free-gate-underdetermines-close]] + the `Nat.strongRecOn` skeleton)
* `ResidualAlreadyProvenInSibling`         — Reflection 358 (a sub-fact a multi-session pointer flags as "the analytically-hard residual" may already be FULLY PROVEN inside a SIBLING lemma over the same substrate that derives it as an intermediate `have` and CONSUMES it for a DIFFERENT deliverable. Authoring the wrapper `nestedSeq_recseqentry_locate`, its LEAF arm needs `h_b : b = off + e.length - 1` (the close-pin) — flagged across R354/355/356 as the analytically-hard residual. But `recseqbody_head_seq_project` (the OLD top-down locate's base case) already proves PRECISELY that uniqueness as its internal `h_uniq : j + 1 = lo + e.length` (a two-sided bracket match: the located floor forbids an earlier close, the entry's own interior floor forbids a later one) and CONSUMES it to build the descended `RecSeqBody` interior. CODE `recseqentry_close_pin` (NonemptyStructure.lean, sorry-free, build green 615 jobs, frontier 4) reuses that prelude VERBATIM through `h_uniq`, then RETURNS the `.seq` decomposition (opener, nonempty interior, RecSeqEntry, head-window identity) + `h_uniq` — exactly the leaf brick's `h_b` + `.seq` shape. The exposure needs a DIFFERENT SHAPE than the sibling's internal use (the sibling built a `RecSeqBody` interior from the same `h_uniq`), so it is extract-AND-repackage, not a verbatim call; duplicating the ~60-line prelude rather than refactoring the sibling is the IRON-RULE-safe choice. The "hard" was an EXPOSURE problem masquerading as a derivation problem. Toy: a two-floor uniqueness `j+1 = L`; `sibling_consumer` derives it as `huniq` and CONSUMES it for a SUFFIX deliverable (`drop (j+1) = []`), `uniq_pin` re-surfaces it in a PREFIX-length shape (`j = L-1`) the second consumer wants; the `#guard`s show the same value serves both shapes, the negative being that the sibling's suffix fact cannot recover the pin. Sharpens [[ref-metric-bridge-is-composition]] + [[ref-reduction-by-import]]; a search-before-deriving win on [[ref-probe-deferred-universal-before-producing]]; complements [[ref-parametric-assembler-extraction]] — the primitive was already produced, just unexported)
* `ComposeArmSeamBeforeSkeleton`           — Reflection 359 (a recursion's branch ARMS are NOT uniform in difficulty; AUDIT each before authoring the `Nat.strongRecOn` skeleton — which call already-landed bricks (compose-only) vs which need infra that does NOT yet exist. Authoring `nestedSeq_recseqentry_locate`, the arm audit found LEAF + ADVANCE compose-only (`recseqentry_close_pin` + `nestedSeq_recseqentry_locate_leaf`; `..._advance` + `seqPathAllSeq_advance`), but DESCEND hides a hard sub-case: a `RecSeqEntry.map` head can't be descended through (no stored `RecSeqBody` interior) and must be REFUTED — and the refutation is NOT one-step `seqPathAllSeq_map_push_breaks` (the carried `SeqPathAllSeq tokens off` is BLIND to `tokens[off]`, and a map head CAN sit at an all-seq-path base, `[{...}]`); it needs a not-yet-existing map-frame-persists lemma. So the full recursion is NOT a safe single increment. The move: COMPOSE the landed bricks of an EASY arm into a standalone arm-callable — the inter-brick SEAM (brick A's output shape reshaped into brick B's input shape) is itself unproven slice/shape algebra, worth landing green in isolation BEFORE the skeleton tangles it with dispatch + measure. CODE `nestedSeq_recseqentry_locate_leaf_full` (NonemptyStructure.lean, sorry-free, build green 616 jobs, frontier 4) composes close-pin→leaf-brick: the window identity `(op::(interior++[cl])) = ((take H).drop off).take L` becomes the prefix decomposition `body = (op::(interior++[cl])) ++ rest` via `List.take_append_drop` (`rest := body.drop L`), the close pin `b+1 = off+L` becomes `h_b`. The new-infra DESCEND map-head refutation is isolated as the next increment. Toy: landed `producer` (shape-A take-equality out) + `consumer` (shape-B append-decomposition in) bricks, `seam` reshapes A→B via `List.take_append_drop` (the new content), `leaf_arm` is the composed standalone callable; `hard_arm` needs an EXTERNAL witness the compose-only bricks can't supply (stays a hypothesis = the deferred obligation); `#guard`s make the seam reshape and the isolated hard arm literal. Sharpens [[ref-from-located-assembler-direction]] (per-arm refinement) + [[ref-structural-moves-complete-recursion]] (moves aren't equal-cost); complements [[ref-parametric-assembler-extraction]] (both primitives exist, the SEAM is the gap))
* `RefuteArmMirrorsPinningSibling`       — Reflection 360 (a flagged "needs-new-infra" REFUTATION arm is often a PINNING sibling's MIRROR: flip the type-discriminator bit, relax the pin-hypothesis to a floor. The R359 audit deferred the DESCEND map-head arm as needing a not-yet-existing map-frame-persists lemma; authoring it (testing that pessimism, [[ref-backward-locator-mirrors-forward]]) showed it was NOT new infra. `seqPathAllSeq_map_frame_persists` (SeqInteriorSeparators.lean, R360, sorry-free, build green 617 jobs, frontier 4) is a near-twin of the EXISTING sibling `seqOpenerType_of_located_and_gate`, which PINS the located opener's type (`tokens[p] = .flowSequenceStart`). Both read the SAME substrate `btFold_frame_inv` (a floored fold from a base whose bottom frame is `bit :: base` returns `m ++ (bit :: base)` — the frame survives). The refutation is the mirror with TWO edits: (1) FLIP the bit `true`→`false` (a `[`→`{`) and conclude the OPPOSITE membership (a surviving `false` ⟹ not all-`true`); (2) RELAX the pin-hypothesis — pinning needs the EXACT body balance (forces the surviving prefix `m = []` so the head is read directly), refuting needs only the FLOOR (the frame is never popped, `m` may be nonempty above the `false`). The substrate already existed for the adjacent consumer; the "new infra" was a parametric twin. Probed true on `[{a: [1]}]` (every position strictly inside the map span `(1,7]` carries a `false`) before authoring. The refuted hypothesis is the window's seq-enclosure `SeqPathAllSeq tokens (a-1)` (R355), making the map-head DESCEND case vacuous (no fifth arm). Toy: shared `FrameInv` substrate (`S = m ++ (bit :: base)`), `pin` sibling (bit `true` + exact `m = []` ⟹ head pinned), `refute` mirror (bit `false`, NO exact hyp ⟹ surviving `false` ⟹ not all-`true`); `#guard`s show the pin's all-`true` stack, the mirror's surviving `false` with a NONEMPTY `m`, and the NEGATIVE (without the surviving frame, `[].all = true` — the floor is load-bearing). Sharpens [[ref-compose-arm-seam-before-skeleton]]; builds on [[ref-near-leaf-mirror-sheds-machinery]] + [[ref-backward-locator-mirrors-forward]]; applies [[ref-metric-bridge-is-composition]])
* `TypeBlindBridgeFalse`                 — Reflection 362 (a balance-only invariant is TYPE-BLIND, so a named "structure ⇒ typed-fact" bridge across a type-blind constructor is FALSE; PROBE a minimal pair before authoring, and on refutation THREAD the typed fact as a parametric-assembler hypothesis. Composing the ADVANCE seam `nestedSeq_recseqentry_locate_advance_step`, the domain brick `seqPathAllSeq_advance` demanded the consumed segment be `WellTyped`. The plan named a discharge — "the head entry is a `RecSeqEntry`, a balanced bracket pair ⇒ `WellTyped`" — and flagged SMALLEST-FIRST to probe it. The probe REFUTED it: `WellBracketed` (the `pbalance` Dyck condition) is type-blind because `flowBracketDelta` maps BOTH `[`/`{` to `+1` and BOTH closers to `−1`, so it cannot tell bracket TYPES apart; and `RecSeqEntry.map` gates its interior ONLY on `WellBracketed`, admitting a mistyped `[ }`. Concretely `{ [ } }` has `pbalance = 0` (passes the gate) yet `btFold (some []) · = none` (NOT `WellTyped`) — ONE `#guard` witness refutes the universal bridge. The move on refutation: do NOT strengthen the shared inductive ([[ref-additive-parallel-type-over-shared-edit]]) and do NOT chase a stronger local invariant (a type-blind invariant cannot CREATE a typed fact, only TRANSPORT one — [[ref-type-blind-invariant-transports-via-converse-frame]]); THREAD the typed fact as a parametric-assembler hypothesis ([[ref-parametric-assembler-extraction]]) in structural form `WellTyped (e ++ [fe])` (slice bridge proven internally), isolating its production as a later frame-transport. Toy: `bal` (type-blind balance, both openers `+1`) vs `tfold`/`typed` (a stack fold that PUSHES which bracket type opened and POPs only the match); `Struct.wrap` gates only on `balOnly`; `#guard`s show the mistyped `{ [ } }` passes `balOnly` but fails `typed`, an `example` builds it as a real `Struct` that is provably `¬ typed`, and `seam` threads `typed` as a hypothesis it cannot manufacture from `Struct`. Sharpens [[ref-probe-deferred-universal-before-producing]] + [[ref-minimal-pair-extracts-the-gate]] (the probe target is a NAMED bridge refuted by one #guard witness, not a ∀-survey); seam context [[ref-compose-arm-seam-before-skeleton]])
* `DeferredTypedFactInGuard`              — Reflection 363 (a seam's deferred "source it LATER by frame-transport" typed-fact obligation is often ALREADY carried by the recursion's existing guard `G` and ALREADY transported by a SIBLING driver over that same `G` — the de-risk is a READ, not a build. R362 threaded the ADVANCE arm's `WellTyped (e ++ [fe])` and filed its production as a residual to be "sourced later by frame-transport from a global `WellTyped`." Two reads resolved it. (1) The guard: the consuming recursion's four-conjunct `G` carries `FlowBodyWindow tokens off H`, and `FlowBodyWindow.wellTyped` IS the WHOLE-window typed fact (`.dyck` the floor) — so the "global `WellTyped` to transport down" is a FIELD of a guard the recursion already descends along, not a fact to manufacture. (2) The sibling: `seqWindowRecSeqBody` (R323), a driver over the SAME `G`, had ALREADY written the exact transport at its own depth-0 cut `m+1` — `WellTyped_subrange … h_win.wellTyped h_bal_m1 (h_win.dyck …)` (lines 1860-1863). The new supplier `nestedSeq_recseqentry_locate_advance_welltyped` (R363, SeqInteriorSeparators.lean, sorry-free, build green 619 jobs, frontier 4) is that four-line transport re-aimed at this seam's slice — same body, different cut. So the ADVANCE arm has TWO landed content pieces (the consume-seam R362 + the produce-supplier R363) and the skeleton owes ZERO content bricks. Toy: `Win` guard BUNDLES the whole-context typed fact as a field `wholeTyped`; `SubrangeTransporter` is the pre-existing transport primitive (modeled as given — re-used, not re-derived); `sibling` calls it on the guard field at one cut and `supplier` re-aims the SAME call at the seam's `e ++ [fe]` (token-identical bodies); POSITIVE = a concrete `Win` whose field holds and whose seam-prefix is genuinely typed; NEGATIVE = the guard field is load-bearing — on the mistyped `{ [ } }` `Win` is uninhabitable, so the typed fact must hold UPSTREAM, never manufactured. Sharpens [[ref-type-blind-invariant-transports-via-converse-frame]]; echoes [[ref-metric-bridge-is-composition]] + [[ref-root-seed-recursive-producer-swap]]; discipline [[ref-compose-arm-seam-before-skeleton]])
* `DemotedDispatchMetricFromStructure`   — Reflection 364 (demoting a recursion's DISPATCH from METRIC-keyed to STRUCTURE-keyed does not delete a downstream arm's metric fact; the fact migrates from "dispatch byproduct" to "structural derivation", and that derivation already exists as an inline block inside the metric-keyed SIBLING — lift it to a standalone brick. The emission-spine-walk locator dispatches by pure LENGTH ARITHMETIC (`move_trichotomy`, R350), "balance demoted to the correctness side, never the decision." But its ADVANCE arm's `WellTyped` supplier (R363) consumes a balance-`0` cut fact `flowBracketBalance tokens off (off+e.length+1) = 0`. In the metric-keyed SIBLING `seqWindowRecSeqBody` this fact (`h_bal_m1`) falls out of the dispatch FOR FREE — it LOCATES the separator `m` by `balance lo m = 0` then adds the comma's delta-`0`. The length-arithmetic locator makes no such call. R364's answer: re-source it STRUCTURALLY — the head entry is a complete `RecSeqEntry` (`pbalance e = 0` via `toWellBracketed`), the `.flowEntry` separator has delta `0`, so `e ++ [fe]` is balanced; and that derivation is NOT new — it is the `h_bal_sep` block already proven inline inside the metric-keyed sibling `recseqbody_advance` (`NonemptyStructure.lean:1169`). The brick `nestedSeq_recseqentry_locate_advance_balance` (R364, SeqInteriorSeparators.lean, sorry-free, build green 620 jobs, frontier 4) is that block lifted standalone, keyed on the locator's slice frame — the last non-mechanical dispatch piece before the closed recursion. Toy: `bal` (type-blind cumulative balance) + a delta-`0` `sep`; `EntryOk` is the structural completeness projecting to `bal e = 0` (toy of `RecSeqEntry`); `metricCut` gets `bal e = 0` from a LOCATED separator (dispatch byproduct), `structureCut` gets the SAME `bal e = 0` from `EntryOk.balanced` (structure) with a TOKEN-IDENTICAL body — the fact migrated, did not vanish; POSITIVE = a concrete complete entry whose cut is `bal (e ++ [sep]) = 0`; NEGATIVE = `EntryOk` is load-bearing — a lone opener (`bal = 1`) cannot build it and its cut is genuinely FALSE. Dispatch dual of [[ref-metric-bridge-is-composition]]; metric analogue of [[ref-deferred-typed-fact-in-guard-via-sibling]]; discipline [[ref-compose-arm-seam-before-skeleton]])
* `AdjacentOriginReconstructs`           — Reflection 366 (a consumer fact at an origin ADJACENT to a carried bundle's origin is NOT a separate guard-debt; it reconstructs by single-token compose across the boundary token whose delta the guard already pins — so itemize the consumer's debt as ONE window-absolute bundle, deriving the lower-origin fact in place. R366 landed the LEAF branch of the emission-spine-walk locator's per-window step `h_step` (`nestedSeq_recseqentry_locate_leaf_typed`, SeqInteriorSeparators.lean, sorry-free, full build green 621, frontier 4). The LEAF seam `nestedSeq_recseqentry_locate_leaf_full` (R359) reads TWO balance facts at DIFFERENT origins: `h_inner` (interior balance `flowBracketBalance (off+1) b = 0`, origin `off+1`) and `h_floor` (the ENCLOSURE floor `∀ i ∈ (off, b], flowBracketBalance off i ≥ 1`, origin `off`, one token LOWER — the opener's own position). A naïve `G` carries both. But `G` is keyed window-ABSOLUTE on the FIXED `[off+1, b)` (R356) via `SeqTypedInterior tokens (off+1) b`, whose interior floor is `≥ 0` at origin `off+1` — no `off`-origin floor. The origin gap is exactly ONE token, the opener at `off` whose delta the guard pins (`tokens[off]! = [` ⇒ `flowBracketDelta = +1`), so `flowBracketBalance off i = flowBracketBalance off (off+1) + flowBracketBalance (off+1) i = (+1) + (≥0) ≥ 1` (`flowBracketBalance_compose` + the single-token opener read) — the `≥ 1` floor is DERIVED, NOT a separate field. So `G` owes the LEAF ONE balance bundle, not two floors. Toy: `bal` (recursive cumulative balance over per-position deltas `δ`); `bal_step` (the single-token compose, toy of `flowBracketBalance_compose`); `floor_lo_from_floor_hi` reconstructs the `≥ 1` floor at origin `lo` from the `≥ 0` floor at `lo+1` + the opener pin `δ lo = 1`. POSITIVE = the reconstruction FIRES on a concrete opener window (`δopen`, opener at `lo=2`); NEGATIVE = LOAD-BEARING on the pin — with a delta-`0` token at `lo` (`δflat`) the interior floor STILL holds but the `≥ 1` enclosure floor FAILS (`bal lo (lo+1) = 0`), so an unpinned/non-adjacent origin IS a genuine debt. Origin-shift instance of [[ref-reconstruct-in-place-over-relocate]]; keeps fields window-absolute per [[ref-window-absolute-gate-subset-restriction]]; the LEAF de-risk of [[ref-width-recursion-combinator-before-grammar-step]]'s SEARCH step)
* `GuardStructureArmByArm`               — Reflection 367 (when pinning a recursion GUARD as a structure, build its field set ARM-BY-ARM: include only fields whose CONSUMER ARM has landed, DEFER a field a not-yet-landed arm needs as an additive extension of the new own-type — the deferral is a de-risk, the arm proof pins the field's exact form, AND free, it is your own type not a shared one. R367 pinned the emission-spine-walk locator's guard as `structure SeqLocateGuard tokens a b off H body` and proved the LEAF disjunct through it (`nestedSeq_recseqentry_locate_step_leaf`, SeqInteriorSeparators.lean, sorry-free, full build green 622, frontier 4). The blueprint named `FlowBodyWindow tokens off H` as a probable field, but only the ADVANCE arm's `WellTyped` supplier consumes it and ADVANCE has not landed — committing it now would GUESS its form (which of its seven fields, whether `2 ≤ lo` holds at the walked windows). So the structure ships LEAF/DESCEND-settled fields only and DEFERS `FlowBodyWindow` to the ADVANCE increment, a free additive extension of the own-type. Landing the LEAF projection PINS seven of ten fields against a real proof. Toy: `GuardBase` carries only the base arm's fields and projects to `Deliv` (`step_base`); when the recursive arm lands it is added as `GuardFull extends GuardBase` and the base projection survives unchanged (`step_base_via_full`). POSITIVE = the base projection type-checks through the extension; NEGATIVE = `guessed_unsat_at_base` — a guard that front-loads the recursive arm's field with a WRONG too-strong precondition `2 ≤ off` is UNSATISFIABLE at the base window `off = 0` even though the real base fact holds, so the guess is unsound until the arm pins it; plus the `rec`-field/`.rec`-recursor name-collision gotcha. Guard-bundle instance of [[ref-additive-parallel-type-over-shared-edit]]; completes fields per [[ref-structural-moves-complete-recursion]]; simplest-arm-first per [[ref-universal-producer-root-seed-first]])
* `StrictContainmentNeedsOpener`         — Reflection 368 (a two-floor relay closes only the NON-strict containment `b ≤ c`; the STRICT `b < c` — the inner close does not coincide with the enclosing interior's OWN end — needs a THIRD input the relay is HEAD-BLIND to: the inner window's START opener at `a-1`. The enclosing gate constrains the interior balance + Dyck floor + the mark at `a`, but says NOTHING about the token at `a-1`, so it cannot tell a genuine entry interior (opener `[`) from a MID-ENTRY SUFFIX (start one-past a separator `,`): inside an interior `x , [y]` the window at `a` = one past the `,` passes the gate with `b` = the enclosing end `c`. So the relay alone yields `b ≤ c`; the opener `bal (a-1) a = 1` upgrades it to `b < c` (it composes to `bal (off+1)(a-1) = -1`, contra the enclosing Dyck floor). The opener is INDEPENDENT of the gate's other fields (the mark gives the stack TOP at `a`, not the token at `a-1`), so it cannot be re-derived downstream — restore it as a window-absolute carrier field that descends free. POSITIVE = `strict_containment`, the abstract relay proved with the opener hypothesis; NEGATIVE = a concrete `[ x , [y] ]` balance where the mid-suffix window `a=3` passes the gate (`bal 3 6 = 0`, floor ≥ 0) with `b = c = 6` yet `bal 2 3 = 0 ≠ 1` (opener absent), while the genuine window `a=4` has `bal 3 4 = 1` and `b = 5 < 6`. Real code: `seqTarget_close_lt_interiorEnd` (SeqInteriorSeparators.lean, R368, the descend re-bundle's `win_hi`). END-dual of [[ref-probe-provider-head-blind-gate]]; complement of [[ref-end-free-gate-underdetermines-the-close]]; relay is [[ref-two-floor-relay-close-bound]]; restore-upstream per [[ref-downstream-derisk-restores-upstream]])
* `ProducerGuardedTrap`                  — Reflection 218 (the producer-guarded-quantifier trap)
* `ConverseForwardAsymmetry`             — Reflection 219 (converse/forward invariant asymmetry)
* `ReductionByImport`                    — Reflection 225 (reduction by import — retype, not shrink)
* `ParametricAssemblerExtraction`        — Reflections 226 + 228 (parametric assembler extraction)
* `ConvergentReduction`                  — Reflection 227 (convergence corollary of reduction-by-import)
* `RecursiveDeliverableProjectToFlat`    — Reflections 234 + 235 (recursive deliverable, project-to-flat-first; single-level descent step)
* `ConsumerJointBeforeProducer`          — Reflection 231 (+ 232 / 237 / 241) (build the consumer joint before the producer; faithful mirror)
* `AdditiveParallelType`                 — Reflection 242 (land a forced deliverable refinement as an additive parallel type, never a shared edit; a dedicated type internalizes the guard)
* `UniversalPackagingJoint`              — Reflection 243 (universal packaging is its own consumer joint — the field/joint hypothesis gap IS the producer's deliverable type; build the assembler before the producer)
* `ProjectionFamilyCompletion`           — Reflection 244 (complete a newly-added deliverable type's projection family before the producer walks it — the omitted projection is the locate's navigation invariant, a verbatim mirror of the sibling's; projections track STORED fields, not desired properties)
* `EmitProducerStrengthening`            — Reflections 249 + 250 (the emit-producer strengthening is itself a consumer-joint-before-producer move at the emit boundary — key the assembler on a SUPERSET per-item predicate carrying the recursive deliverable (`RecEntry`, unrecoverable from the flat `FlatEntry`, witnessed by `flatentry_aa` + `not_recentry_aa`); and the recursive producer is a verbatim mirror of the flat one over ONE shared induction, only the leaf constructor swapped — `buildRecBody` is `buildFlatBody` with `RecBody.single/.cons` for `FlatBody.single/.cons`, `recbody_to_flatbody` projecting rec ⟹ flat. **R250 map layer**: the same strengthening mirrored across a SECOND axis (seq→map) — `RecPair` REUSES `RecEntry` verbatim for both key and value slots (predicate reuse), `buildRecMapBody` is `buildFlatMapBody` with the leaf swapped AND `buildRecBody` with the per-item predicate swapped (the two-axis mirror), and `recpair_len_floor` (`3 ≤ length`) is the load-bearing map-side scaffolding the seq body lacks — keep it, mirror the same-axis parent not the other-axis sibling)
* `ProducerDualOfConsumer`               — Reflections 245 + 246 + 247 + 248 (the producer's per-level assembler is the constructive dual of the consumer joint — same positional bridge, opposite direction, constructor vs. eliminator; its symmetric MIRROR transports the plumbing verbatim but sheds at the constructor exactly the field the additive parallel type projects instead of stores: seq `SEntry.seq` stores `WB`, map `MEntry.map` omits-and-projects it; the BUNDLE assembler `bundleLocatedSeq`/`bundleLocatedMap` lifts the field-level dual to the consumer's bundled deliverable `SLocated`/`MLocated` — only the recursive `entry` field is non-trivial, the rest are window-guard pass-throughs, and the map bundle threads one extra stored primitive `keyF` the entry producer never supplies; and R248 — that storage asymmetry is SCALE-FREE: the same projected-vs-stored mechanism SHRINKS the entry constructor and GROWS the bundle, opposite signs, witnessed by `wb_recovered` (post-hoc projection at the shrunk constructor) vs `mlocated_key` (the grown-in stored field projected back out))
* `FreeProjectionInvariant`              — Reflections 253 + 254 (a consumer-bundle field that is definitionally a CONJUNCT of a property the recursive deliverable already PROJECTS to is a FREE field, not a threaded obligation — read the deliverable's projection family before deciding what the recursion threads: `WB l := balance l = 0 ∧ ∀ i, 0 ≤ balance (l.take i)`, the recursive `Rec` projects to `WB` (`Rec.toWB`, `wrap` case = `WB_wrap`), so the bundle `Located`'s `dyck` field is `(Rec.toWB h).2` for free — `located_of_rec` takes only the `wt : WT` hypothesis and no `h_dyck`; the CONTRAST is `WT` (typed bracket matching), which `Rec` does NOT project to — `rec_mismatched : Rec [os,a,cm]` holds yet `not_wt_mismatched : ¬ WT [os,a,cm]`, so `dyck_mismatched_free` is still derivable but no `Located [os,a,cm]` is — the R244 stored-vs-projected split surfacing at the locate's invariant SET rather than a constructor's fields. **R254**: the one invariant with no local projection (`WT`) is recovered all the same — *non-locally*, from the OUTER window's `WT` via a subrange interface `WTsub` (the toy of `WellTyped_subrange`) whose two side conditions are the window's *free* balance + Dyck off `Rec.toWB`; the outer assembler `located_of_rec_outer` (toy of `seqLocated_of_recseqbody_outer`) threads ONE outer `WT`, recovers the window's `wt` through `WTsub`, and takes no per-window `WT` and no `dyck` — only the structural `Rec`; `wtsub_concrete`/`located_window_via_outer` witness it on `{ { a } } ⊃ { a }`, so Dyck is free LOCALLY and `WT` is free NON-LOCALLY, neither threaded per window)
* `StoredVsProjectedRecursionEdge`       — Reflections 251 + 252 (a recursive-deliverable inductive's STORAGE choices set the PRODUCER's recursion graph — `Entry.seq` stores the recursive `Body interior` so the producer's `seq` case recurses (`buildEntry` → `buildBody`), while `Entry.map` projects only the flat `bal interior = 0` so the producer's `map` case is a LEAF (`buildEntry` → `bal_emitList`, never `buildBody`), severing the recursion edge; witnessed by `mapLeaf` (a flat leaf builder for the projecting member, with no `seqLeaf` counterpart) and by `built_map_empty : Entry [om,cm]` (the projecting member accepts an interior with NO `Body`, even though `not_body_nil` shows `Body []` is uninhabited and the storing member therefore needs a dedicated `seqEmpty`); the producer targeting only projecting members is strictly smaller and dependency-light — a single self-contained induction — and in a producer family the projection severs the cross-producer edge, ordering the family. **R252**: the *dependent* member's IH is dead — `KeyEntry e := Entry e ∧ bal e = 0` (a saved-key-style twin deliverable) is built by the *non-recursive* `buildKeyEntry := ⟨buildEntry v, bal_emit v⟩`, which routes the recursive `Entry` part to the BASE sibling `buildEntry` and the extra flat fact to `bal_emit`, never calling itself — so the base carries the family's only live self-recursion and each dependent consumes it as a black box; `key_projects_to_entry` recovers the base `Entry` back out)
* `BoundaryBoundRecovery`                 — Reflections 255 + 256 (a consumer-joint-before-producer move has TWO boundaries — when the consumer quantifies a window UNBOUNDEDLY but the producer/assembler needs positional BOUNDS, hoist the bound-recovery into one joint that recovers the bounds ONCE from boundary invariants the enclosing context already owns, rather than re-proving them at every recursive call: `bound_lo` derives `2 ≤ lo` from the stream-start token `toks[0]! = SS` + the opener guard `toks[lo-1]! = OB` (the `OB ≠ SS` distinctness rules out `lo ≤ 1`, where `lo-1 = 0`), `bound_hi` derives `hi ≤ length-2` from `toks[length-1]! = SE`, and `locator_of_window_recbody` (toy of `seqLocator_of_window_recseqbody`) collapses the unbounded locator to the bounded per-window producer `h_rec`; the boundary token is load-bearing — `bad = [OB,A,CB]` with no `SS` frame satisfies the opener guard at `lo = 1` yet `¬ (2 ≤ 1)`, the window the missing boundary token would have excluded, witnessed by `neg_no_ss`. **R256 map mirror**: `bound_lo_map`/`bound_hi_map` are the verbatim twins over the map brackets `MB`/`ME`, reading the SAME `SS`/`SE` frame tokens (the recovery is bracket-agnostic — the stream frame does not know which collection a window opens); the R246 storage asymmetry surfaces as ONE extra per-window producer hypothesis `h_prim` (toy of the six pair primitives) on `locator_of_window_recmapbody`, which recovers the bounds ONCE and feeds them to BOTH `h_rec` and `h_prim` — `pos_lo_map`/`pos_hi_map`/`mapPrim goodMap 2 3` positive, `neg_no_ss_map` negative)
* `FoldConsumerChain`                     — Reflection 257 (fold a joint-by-joint consumer chain into ONE lemma keyed only on the producer's contract, before writing the producer — the fold costs nothing and cannot regress but converts a scattered residual into one typed boundary: `jBound` (boundary-anchoring, recovers `2 ≤ lo` from the frame token `toks[0]! = SS`) and `jPack` (universal packaging) compose into `targetOfProducer`, keyed only on the abstract producer `hProd` and the in-scope frame fact `hSS`; `located_via_fold` shows an arbitrary-window `Located` then follows from the producer + frame alone; positive `prod_good : Producer good 2 3 := by decide` on `good = [SS,OB,A,CB,SE]`, negative `neg_no_ss` on `bad = [OB,A,CB]` where the opener guard holds at `lo = 1` yet `2 ≤ 1` is false — the frame token is load-bearing for the fold)
* `UniversalProducerRootSeed`              — Reflections 258 + 259 (a universal producer's first brick is its ROOT SEED — the positional packaging of its one already-available instance, built as a faithful mirror of the flat-deliverable characterization with the payload swapped: once the consumer is folded (R257), the producer starts at the recursion's base case, not the recursion. `charRec` is the verbatim mirror of `charFlat` — same `subst`/`drop_pre` body, the flat payload `Flat` swapped for the recursive `Rec`; `root_instance` shows the seed IS the `lo = pre.length` (outer-window) instance of the universal `UnivRec`, the rest deferred to the navigation recursion (so `UnivRec` is left open). Positive `rec_good`/`seed_root` on `good = [OB,A,CB]` with a length-1 prefix recovered by `drop 1`; negative `not_rec_empty` on `empty = []` via the `Rec ⟹ Flat` projection — the recursive deliverable can never produce an empty block, so the seed never fires there. **R259 map extension**: the seed mirrors verbatim across the seq→map collection axis — `charMap` is the *verbatim* copy of `charRec` (payload `Rec` swapped for the parallel `RecMap`), the positional packaging step being collection-agnostic; the only deltas live in the *feed*, the R246 storage asymmetry one tier up at the seed's hypothesis count — `MapFeed` carries an extra `ska` precondition (toy of `simpleKeyAllowed`) and an extra `floor : 3 ≤ length` (toy of the R250 `3 ≤ n` map floor), BOTH absent on `SeqFeed`, and `seedMap` ignores them exactly as the real lemma ignores `_h_n3`. The floor is load-bearing: `seqfeed_singleton : SeqFeed [A]` is inhabited but `no_mapfeed_singleton : ¬ MapFeed [A]` (`3 ≤ 1` false), the seq side having no such floor; positive `seed_map_root` on `good`, negative `not_recmap_empty` via the `RecMap ⟹ Flat` projection)
* `DescendStepConsumerJoint`                — Reflections 260 + 261 (the navigation recursion's *descend step* IS a consumer joint's internal computation, truncated: returned as a value instead of consumed — and its symmetric map mirror. A consumer joint that peels/decomposes a located entry, descends one nesting level, then *consumes* the descended sub-deliverable into a terminal already contains the producer-side descend primitive its dual recursion needs — the recursion needs that sub-deliverable *as a value* for its IH, not the consumed terminal, so the descend step is the same proof body truncated before the consume step. Toy `RecEntry`/`RecBody` (mutual): `seq_interior` is the shared single-level descent; `recbody_of_entry` is the descend primitive returning `RecBody interior ∨ interior = []`; `flat_of_entry` is the consumer joint — defined literally as `(recbody_of_entry …).imp RecBody.toFlat id`, so the factoring is manifest: the joint = the descend primitive followed by the `toFlat` consume step, the primitive = the joint with that step truncated. Positive `descend_one` returns the recursable `RecBody [A]` while `consume_one` keeps only the projected `Flat [A]`; `descend_twice` descends the doubly-nested `[OB,OB,A,CB,CB]` two levels using only the primitive (the joint's `Flat` cannot be re-descended); negative `no_descend_scalar` — a scalar entry `[A]` has no `OB :: (… ++ [CB])` bracket shape, so the descent never fires. **R261 map mirror** (`## The map mirror` section): `MapEntry`/`MapBody` is the seq toy with NO scalar constructor — every entry is a bracket frame — so `map_interior` is *one case shorter* than `seq_interior` (no scalar to exclude), the toy reflection of the real map descend mirror's *dropped `h_op` opener-guard hypothesis*; `mapbody_of_entry`/`flat_of_map_entry`/`descend_map_one`/`descend_map_twice` are verbatim mirrors of the seq witnesses, and negative `no_mapentry_scalar` — a bare scalar `[A]` is not a `MapEntry` at all (the structural fact behind the dropped guard))
* `StructuralMovesAdvance`                  — Reflection 262, 263, 269 & 270 (the locate recursion's *structural moves* — descend + build + advance + terminate — one positional window lift per CONSTRUCTOR of the deliverable, base case included. A recursion producing a recursive-deliverable inductive needs exactly one positional move per *constructor* of the deliverable — not merely per way the constructors compose; for a body-of-entries deliverable (`single`/`cons`) that is DESCEND (into a nested entry's interior, eliminator side), BUILD (the same composition, constructor side — descend's dual), ADVANCE (chain entries via `RBody.cons`), and TERMINATE (the `single` base constructor). Once every constructor's lift lands — each a cheap verified brick — the recursion's residual collapses to the purely *analytical* entry-boundary location (the split points the moves consume). Subsidiary: the `RBody.cons` side-fields `h_ne`/`h_head` are free structural projections of the entry (`REntry.ne_nil`/`REntry.head_cstart`), not threaded obligations. Toy `REntry`/`RBody` (mutual) with a real `cons` body constructor: `recbody_of_entry` is DESCEND, `entry_of_recbody` is BUILD, `recbody_cons` is ADVANCE (discharging the two projections). The positives compose all moves on a *multi-entry* nested body — `body_AFA` builds `[A,FE,A]` by ADVANCE, `entry_AFA` wraps it by BUILD into `[OB,A,FE,A,CB]`, `descend_multi` DESCENDs back to the multi-entry interior, `body_full` ADVANCEs again; negatives `no_entry_empty`/`no_entry_separator` — an entry is never empty and never a bare separator, so the moves never misfire. **Extended by Reflection 263** with the *map mirror* of the advance step: a toy `MBody`/`MPair` (a body chains *pairs* `KY :: (block_k ++ VL :: block_v)` along `FE`), `mbody_cons` is the verbatim seq→map mirror of `recbody_cons` — only the terminal constructor (`MBody.cons` over an `MPair`) and the head side-field differ; the R246/R263 storage asymmetry surfaces as the *simpler* head projection `MPair.head_key` (one `cases`, single equality `= KY`, vs `REntry.head_cstart`'s four-case `CStart` disjunction); positives `pairAA`/`mbodyAA`/`mbody_two` (ADVANCE builds a two-pair body), negatives `no_mpair_empty`/`no_mpair_no_key` — a pair is never empty and its head is pinned to `KY`. **Extended by Reflection 269** with the *TERMINATE* move the original trio missed: `RBody` has two constructors (`single`/`cons`) but only `cons` had a lift, so the recursion had a step and no base; `recbody_single` lifts the `single` base constructor — the move ADVANCE defers to (its `h_rest` is dischargeable only at the last item). It is the cheapest move: `RBody.single` is indexed by the entry list *verbatim*, so it needs **no** positional plumbing (vs `recbody_cons`'s `e ++ FE :: rest`) and is axiom-clean `[propext]` — the complexity law that a move's cost = the gap between its constructor's index and the raw window, here nil. `body_AFA`/`body_AFAFA` now show the genuine recursion shape ADVANCE ∘ … ∘ TERMINATE; `mbody_single` is the verbatim map mirror, the R263 storage asymmetry recurring at the base move's head projection. **Extended by Reflection 270** (the map TERMINATE move's real-code landing, `recmapbody_single_window`): a base-case mirror is *free* — a mirror's cost is the plumbing it transports, and a base case has none, so the map terminate differs from the seq one only in the leaf constructor + projection family (axiom profile read off the original: both `[propext]`). `mbody_PFPFP` is the map-side recursion-shape witness — ADVANCE ∘ ADVANCE ∘ TERMINATE over a three-pair body, the structural twin of the seq `body_AFAFA`, identical move composition with `MBody`/`MPair`/`mbody_*` swapped for `RBody`/`REntry`/`recbody_*`)
* `TwoFloorRelayCloseBound`                — Reflection 314 (a located matching-close's containment bound is a TWO-FLOOR RELAY — a single underflow witness at `close+1`, refuted by whichever of two ADJACENT-domain floors tiles it — the toy of `seqClose_of_located_and_enclosing`. The descent locates the enclosing opener `p` and its matching close `j` (`balance (p+1) j = 0`, close delta `-1`); the consumer needs the close to CONTAIN the gated window `[a,b)` (`a ≤ j ∧ b ≤ j`), but the matching-close locator only returns `j < hi`. The bounds come from the consumer's OWN floors. The close underflows the next step for ANY base `β ≤ j`: `balance β (j+1) = balance β j − 1`. The span `[p+1, b]` is TILED by two separately-sourced floors — the locator floor over `[p+1,a]` and R313's gate floor over `[a,b]` — and you case-split on which domain `close+1` lands in: `a ≤ j` (else `j+1 ≤ a`, locator floor gives `balance (p+1)(j+1) = −1 < 0`, contradiction), then `b ≤ j` (else `j+1 ≤ b`; having `a ≤ j` ⇒ `a ≤ j+1`, gate floor gives `balance a (j+1) = −1 < 0` with `balance a j = 0` by composition). No unified floor needed — adjacent floors that tile suffice; the first bound is the ordered GUARD for the second floor's lower edge. **PROVEN** (`located_close_bounds`, the abstract nugget over any `bal` with additivity + the close-underflow step): `a ≤ j ∧ b ≤ j`. **POSITIVE (`#guard`)** on `W = op op ct cl op ct cl cl` (`[[1],[2]]` skeleton, first inner seq `p=1`, close `j=3`): `bal 2 3 = 0`, `d W[3] = −1`, `bal 2 4 = −1` (the close+1 underflow), genuine window `[2,3)` has `a=2 ≤ 3 ∧ b=3 ≤ 3`. **NEGATIVE (`#guard`)**: the cross-sibling `[2,6)` is balanced but dips to `bal 2 4 = −1` (gate floor over `[2,6]` fails) and `6 > j=3` — so `b ≤ j` would be FALSE without the gate floor. This is the general mechanism behind R313's "the floor discharges `b ≤ hiS` for free", generalized to two floors ([[ref-two-floor-relay-close-bound]]); the seq-specialized `flowBracketBalance_matching_close_seq` already bundled matching-close + typed-close (search the SPECIALIZED packaged lemma before composing the generic primitives, echoing [[ref-metric-bridge-is-composition]]))
* `NarrowFromRootBreaksRederivationCycle`  — Reflection 318 (re-deriving a SUBSET-RESTRICTION carrier per recursion-window is CIRCULAR if its per-window input comes from the recursion's own output; seed it ONCE at the root from the FLAT producer and narrow down — the toy of `seqRoot_seqInteriorSeparators` and the de-risk that corrected R317. The seq `windowWidth_strongRecOn` step needs `SeqInteriorSeparators tokens lo hi` per window (it supplies the separator facts `flowBodyContent_of_deep` cannot project). R317 planned to RE-DERIVE the carrier per window via a dispatcher needing that window's own `SafeBodyUnit` — but at a descended window the only `SafeBodyUnit` source is `RecSeqBody.toSafeBodyUnit` of that window's own `RecSeqBody`, the VERY OUTPUT the step produces: circular. The carrier is a subset restriction (`SeqInteriorSeparators_narrow`), so seed it once at the root from the FLAT `seqRoot_safeBodyUnit` (no recursion ⇒ no cycle) and narrow to every sub-window; the per-window helper (`RecSeqBody.toSafeBodyUnit`, the circular edge) is DROPPED. **PROVEN nugget**: `C lo hi := ∀ a b, lo ≤ a → a ≤ b → b ≤ hi → Q a b`; `C_narrow` (the subset-restriction edge, two `Nat.le_trans`); `C_all_of_root` (root `[0,N]` ⇒ every `[lo,hi]` with `hi ≤ N`); `flat_root` (the root seed, established directly with no recursion). **POSITIVE** (`example : C 2 4`): the root seed at `[0,5]` covers the nested sub-window `[2,4)` for free (the de-risk coverage claim — the window lies WITHIN the root span). **NEGATIVE (`#guard`)**: model the step `P w := step (C w)` reading the carrier; the FLAT path `pFromFlat w = step (flatC w)` is window-AWARE (`pFromFlat 3 = 4`, `pFromFlat 7 = 8`), while the CIRCULAR re-derivation `pCirc` (`C w := extract (P w)`, `P w := step (C w)`, fuel-exposed) is window-BLIND — `pCirc 100 3 = pCirc 100 7 = 1`, the same regardless of `w` and never the genuine value (`!(pCirc 100 3 == pFromFlat 3)`): the cycle injects no real data, only the flat root seed carries the per-window datum. Circularity rider on [[ref-universal-producer-root-seed-first]] + [[ref-window-absolute-gate-subset-restriction]] ([[ref-narrow-from-root-breaks-rederivation-cycle]]))
* `ImportForcedParametricGuard`            — Reflection 322 (thread a recursion guard through an IH-supplying lemma that is UPSTREAM of the guard's definition by making the upstream lemma PARAMETRIC over an abstract predicate `Q` — the toy of the R322 `Q`-parametric IH re-versioning across `recseqentry_seqbracket_oracle` / `recseqentry_window_dispatch` / `seqChild_safeBodyUnit` / `seqDescent_provider_of_located`. The real blocker: the seq recursion must thread `SeqEnclosed tokens lo` through its IH, but the oracle/dispatch live in `NonemptyStructure`, UPSTREAM of `SeqEnclosed` (`SeqInteriorSeparators` imports `NonemptyStructure`) — so the oracle CANNOT name the guard, and the de-risk's "edit the IH type in place" plan is impossible. Modeled with two namespaces: `Upstream` (declared FIRST — `oracle`/`dispatch` parametric over `Q : Nat → Prop`, taking the descend output `Q (lo+1)` as an OPAQUE hypothesis, naming no concrete guard) and `Downstream` (the concrete `Enc n := 1 ≤ n`, its push-blind descend `enc_descend`, and the `fixpoint` that binds `Q := Enc` — the ONLY site that names the guard, filling the opaque hyp with `enc_descend`). POSITIVE: `fixpoint 2 5 _ : RecBody 2 5` builds the deliverable end-to-end via `widthRec` (a self-contained `windowWidth_strongRecOn` toy), the step's width-IH re-packaged into the dispatch's `Q`-parametric IH verbatim. NEGATIVE: `enc_not_universal : ¬ ∀ n, Enc n` (`Enc 0` false) — a guard-BLIND adapter cannot INVENT `Q lo'` for a general `lo'`, so the guard must be THREADED, never dropped. The de-risk lesson: check the IMPORT DIRECTION of the guard's definition vs the IH-supplying lemma before assuming an in-place IH edit is local. Sharpens [[ref-parametric-assembler-extraction]] (the universal is over the GUARD predicate, forced by imports); complements [[ref-additive-parallel-type-over-shared-edit]] (GENERALIZE the upstream lemma, don't specialize it); the descend output is the [[ref-push-blind-frame-dependent]] (R321) edge supplied at the instantiation site ([[ref-import-forced-parametric-guard]]))
* `VacuousPositiveCannotExclude`           — Reflection 323 (a guard/carrier fact of shape `prem → pos` proven VACUOUSLY — its premise refuted internally — gives the consumer ONLY `pos`, never `¬ prem`, so it CANNOT exclude `prem` downstream when `pos` is consistent with the `prem`-case; thread a DISCRIMINATOR that directly contradicts `pos`. The toy of `seqWindowRecSeqBody`'s no-trailing-separator obligation `m + 1 < hi`: the carrier's `noTrailingSepFact` is `(window ends on a depth-`0` separator) → isFlowContentStart tokens[hi]`, proven vacuously (no trailing comma), but its conclusion is CONSISTENT with the trailing-comma window the three-conjunct `G` genuinely admits — so the carrier alone cannot rule out `m + 1 = hi`. Modeled with a tiny `Tok` alphabet (`content`/`sep`/`close`), the vacuous-positive `noTrailingSep lastIsSep bnd := lastIsSep → isContentStart bnd`, and the window-END discriminator `windowEnd bnd := bnd = .close`. **NEGATIVE** (`carrier_cannot_exclude`): `∃ lastIsSep bnd, noTrailingSep lastIsSep bnd ∧ lastIsSep` — take `bnd = .content`, the positive holds WHILE the bad case holds, so the carrier has no exclusion power. **POSITIVE** (`exclude_with_discriminator`): `noTrailingSep` PLUS `windowEnd` (the discriminator, via `close_not_contentStart : windowEnd → ¬ isContentStart`) yields `¬ lastIsSep` — exactly the O1 discharge, where the fourth `G`-conjunct `tokens[hi]! = .flowSequenceEnd` collides with the boundary `isFlowContentStart tokens[hi]`. Sharpens [[ref-probe-deferred-universal-before-producing]] (a draft-`sorry` on the suspected obligation surfaced the gap the plan narrative hid); complements [[ref-downstream-derisk-restores-upstream]] (the window-END invariant restored into the guard / IH-thread, not the producer body) ([[ref-vacuous-positive-cannot-exclude]]))
* `PushBlindFramePreserveDependent`        — Reflection 321 (an accumulator-guard threaded down a recursion has TWO edges with OPPOSITE parent-dependence — the toy of `SeqEnclosed` and its edges `seqEnclosed_descend` / `seqEnclosed_advance`. The guard reads the TOP of an accumulator stack (`Enclosed s := s.bind (·.head?) = some true`, toy of the enclosing btFold-top fact). **DESCEND = PUSH** (`push b s := s.map (b :: ·)`, toy of `btStep` on an opener): pushing a new frame OVERWRITES the top, so the edge is parent-value-BLIND — `descend_push` needs only `∃ l, s = some l` (the parent is DEFINED), NOT that the parent is `Enclosed`. **ADVANCE = FRAME** (`frame s := s`, toy of `WellTyped_frame`: a balanced segment returns the accumulator unchanged): the top is PRESERVED, so the edge is parent-value-DEPENDENT — `advance_frame` needs the parent `Enclosed` and carries it forward. **PROVEN nugget**: `descend_push` (from definedness alone, `rfl` after `obtain`) and `advance_frame` (`:= h`, the frame preserves). **POSITIVE/NEGATIVE (`#guard`)** exhibiting the asymmetry: over the SAME non-enclosed parent `some [false]` (top `false`), the PUSH edge ENCLOSES (`isEnclosed (push true (some [false])) = true` — blind to the parent) while the FRAME edge does NOT (`isEnclosed (frame (some [false])) = false` — cannot manufacture enclosure), so `isEnclosed (push true (some [false])) != isEnclosed (frame (some [false]))`; and `isEnclosed (push true none) = false` shows DESCEND still needs DEFINEDNESS. Distinct from [[ref-converse-forward-invariant-asymmetry]] (property vs CONVERSE); the accumulator-guard analogue of R288's `GuardEdgeFloorAsymmetry` (balance guard's edges need different FLOORS — here the split is whether the parent's value is read at all) ([[ref-push-blind-frame-dependent]]))
* `FloorBlindGateAdmitsCrossSibling`      — Reflection 313 (a FLOOR-BLIND gate — depth-`0`-balanced only — admits CROSS-SIBLING windows the deliverable cannot inhabit, so the carrier is FALSE on a valid witness; the missing local-Dyck floor excludes EXACTLY those windows and discharges the consumer's containment bound `b ≤ hiS` for free — the toy of the R313 strengthening of `SeqTypedInterior`. The forward-CLOSE brick's de-risk found that the gate `balance a b = 0 ∧ seq-enclosed` admitted the cross-sibling window `[3,7)` of `[[1],[2]]` (from inside the first inner seq to inside the second), on which `bodySuccFact` is FALSE — so `SeqInteriorSeparators` was false on a valid value. Toy alphabet `op`/`cl`/`ct` (`+1`/`-1`/`0`); witness `W = op op ct cl op ct cl cl` (skeleton of `[[1],[2]]`); `bal` the running balance. **NEGATIVE (`#guard`-backed)**: window `[2,6)` has `bal 2 6 = 0` (passes the BARE gate) yet DIPS to `bal 2 4 = -1` (floor violated) and `bodySucc [2,6)` is FALSE (`ct` at `2` is depth-`0`-complete but `W[3] = cl`, not a separator); the floor SEPARATES it. **floored ⟹ bodySucc** at every balanced sub-window (`#guard`-enumerated), and a genuine entry window `[2,3)` is floored AND satisfies bodySucc. **POSITIVE (proven, `floored_window_cannot_cross`, `[propext, Quot.sound]`)**: a window satisfying the local-Dyck floor cannot cross a close that underflows its balance — `bal a (m+1) < 0` contradicts the floor at `m+1` — which is exactly the consumer's `b ≤ hiS = j` for free. This is [[ref-probe-provider-head-blind-gate]] for a FLOOR-blind gate, and the gate-domain dual of [[ref-downstream-derisk-restores-upstream]] (R311 restored a dropped PRODUCER conjunct; here the missing conjunct is in the GATE). Sharpens [[ref-window-absolute-gate-subset-restriction]] (the floor is window-absolute, so the carrier stays a subset restriction))
* `TypeBlindInvariantTransportsViaConverseFrame` — Reflection 312 (a TYPE-BLIND numeric invariant (the floor) cannot CREATE a typed fact — fold-definedness — only LICENSE its transport across a frame boundary; moving definedness from a verified LARGER context to a SMALLER sub-context needs the CONVERSE of the forward framing lemma, not the forward one — the toy of `seqOpenerType_of_located_and_gate` / `btFold_frame_inv`. The 159th "Next step" predicted route (B): prove the singleton-frame interior fold returns to `[b]` *from the floor*, then extend by the FORWARD frame. Authoring corrected it. A 5-symbol typed-bracket alphabet (`os`/`om` push `true`/`false`, `cs`/`cm` pop a *matching* head or fail, `nt` neutral); `step`/`fold` the typed machine, `pbal` the numeric balance. **Correction 1 (NEGATIVE, `#guard`-backed — the floor is type-blind)**: `[os, cm]` (the `[ }` analog) has every prefix balance `≥ 0` and total `0` (passes BOTH the floor and balance-0 that route (B) relied on) yet `fold (some []) [os, cm] = none` — the `}` cannot pop the `[`; so no floor/balance reasoning gives fold-definedness, a typed fact only the real stream supplies. **Correction 2 (POSITIVE, the forward/converse asymmetry)**: `fold_frame` (forward) needs NO numeric hypothesis (carries a proven SMALL fold up); `fold_frame_inv` (the CONVERSE) DOES need the floor (transports DEFINEDNESS down — dropping frame elements would underflow unless the floor forbids it). `step_frame_inv`'s `0 ≤ s.length + delta t` is the no-pop-into-frame guard; the floor re-relativizes per step via `step_length`. **The brick (`opener_bit_via_converse_frame`)**: the gate gives `fold (some (b::s_p)) interior = some S` (definedness in the LARGE context), `fold_frame_inv` (+ floor) pushes it down so the interior folds from `[]` to length-`pbal`-`= 0` = `[]`, hence `S = b :: s_p` — head `= b`, fixed by the gate. A property and its converse over one structure need DIFFERENT invariant strengths — forward free, converse floor-guarded ([[ref-converse-forward-invariant-asymmetry]]); the de-risk's route-(B) error was expecting the floor to do the FORWARD lemma's job. Sharpens [[ref-probe-deferred-universal-before-producing]])
* `DownstreamDeriskRestoresUpstream`        — Reflection 311 (a downstream consumer's de-risk can FALSIFY a deliberately-narrowed UPSTREAM deliverable; when a minimal pair shows the dropped conjunct is the discriminator AND it is independent of the producer's other outputs, restore it in the PRODUCER — the toy of the floor restored to `flowBracketBalance_backward_open_locate`. R309 sized the locator's deliverable DOWN to its rebase consumer, dropping the Dyck FLOOR; R311 is the sequel — the opener-TYPE consumer re-opens it. Tokens are a delta stream `d : Nat → Int` with opener type `ty : Nat → Bool`; `bal` is cumulative-difference balance, so composition is free. **Correction 1 (NEGATIVE, `#guard`-backed)**: the toy of `[{}, ["9"]]` (deltas `[1, 1, -1, 0, 1, 0]`) at window start `a = 5` admits BOTH `p = 4` (the true innermost `[`, `ty = true`) AND `p = 1` (a spurious `{`, `ty = false`, body `} , [` nets `0`) under the THREE BARE facts (`p < a`, `d p = 1`, `bal (p+1) a = 0`) — and they DISAGREE on type (`ty 1 ≠ ty 4`), so no consumer of the bare existential can pin `ty p`; the FLOOR separates them (`bal 5 5 = 0 ≥ 0` vs `bal 2 3 = -1`). This sharpens R309's `[ ] [` pair (same TYPE there; DIFFERENT type here — which is exactly why the *type* brick forces the floor back). **Correction 2 (POSITIVE `floor_unique`)**: adding the floor makes the located opener UNIQUE (any two floored witnesses coincide) by pure balance composition — if `p < q` then `bal (p+1) (q+1) = 0` yet `= bal (p+1) q + d q = (≥0) + 1`, absurd; uniqueness ⇒ the gate's innermost type IS `ty p`. The floor is INDEPENDENT of the three facts (the minimal pair satisfies them yet only `p = 4` is floored), so it cannot be re-derived at the consume site — the producer must deliver it. The DUAL of [[ref-backward-locator-mirrors-forward]] correction 2's size-DOWN: a deliverable is sized to the UNION of consumers, and a new consumer's de-risk can size it back UP. Sharpens [[ref-minimal-pair-extracts-the-gate]] and [[ref-probe-deferred-universal-before-producing]])
* `MetricBridgeIsComposition`               — Reflection 310 (a feared "bridge between two PARALLEL metrics" is often ALREADY a theorem: a STRUCTURAL count and a NUMERIC count that share a substrate are equated by COMPOSITION, not a new per-step induction — the toy of `flowBracketBalance_pos_of_btFold_head`, the gate→hypothesis bridge of `(i'-b-locator-glue-gate-bridge)`. A token stream (`op b` pushes a `Bool`, `cl` pops, `neu` neutral) carries two metrics: the NUMERIC `bal` (fold of `{+1, -1, 0}`) and the STRUCTURAL `fold` (a `List Bool`, its LENGTH the metric). The de-risk feared they might DIVERGE (push map vs seq differently), needing a per-step induction relating pushes to deltas. **Correction 1 (POSITIVE `bridge`)**: they do not — both reduce to the SAME substrate `bal` via `foldLen` (`stack length = bal`), so `bridge` (non-empty stack ⇒ `bal ≥ 1`) is `foldLen` + "non-empty ⇒ length ≥ 1" + `omega`; the per-step reconciliation the de-risk feared IS `foldLen`, which already exists. **Type-agnostic dividend (POSITIVE)**: `bridge` takes `= some hd` for an ARBITRARY `hd : Bool` — the proof is blind to the head value, so ONE lemma serves both axes (`bridgeTrue`/`bridgeFalse`, seq/map). **Correction 2 (NEGATIVE, `#guard`-backed)**: the fear is legitimate IN GENERAL — a WRONG metric `wfold` (pushes only on `op true`, ignoring `op false`) genuinely diverges: on `diverge = [op false, op true]`, `#guard (fold …).map (·.length) == some 2` and `bal == 2` (real metrics coincide) but `#guard (wfold …).map (·.length) == some 1` (≠ `bal`). So VERIFY the substrate is shared (every `op _` pushes uniformly AND contributes `+1`); don't assume divergence (author an induction) NOR coincidence (skip the check). Sharpens [[ref-probe-deferred-universal-before-producing]] on the metric-unification axis, as [[ref-backward-locator-mirrors-forward]] did on the machinery axis)
* `BackwardLocatorMirrorsForward`           — Reflection 309 (a backward locator mirrored from a forward one needs NO standalone backward-matching-open primitive, and its DELIVERABLE is sized to the consumer's reads — the toy of `flowBracketBalance_backward_open_locate`, the HARD CORE of the `(i'-b-backward-locator)` descent. Tokens are abstracted to a delta stream `d : Nat → Int` (each delta in `{-1,0,1}`); `bal d n` is the running balance from `0`. **Correction 1 (POSITIVE `backwardLocate`)**: the locator `∃ p, p < a ∧ d p = 1 ∧ bal d (p+1) = bal d a` is proved by `Nat.strongRecOn` on `a`, casing the last delta `d (a-1)` into opener/neutral/closer; the closer case does NOT author a standalone backward-matching-open lemma (the mirror of a forward matching-close scan) — the IH at `a-1` IS the closer's matching opener `p'`, and a one-line `pairSkip` (`bal d a = bal d p'`, toy of `flowBracketBalance_bracket_pair_skip`) jumps the matched block, then the IH at `p'` walks outward. The recursion structure IS the backward matching-open; mirroring a locator backward SHEDS machinery ([[ref-near-leaf-mirror-sheds-machinery]] on the matched-block-skip axis), it does not add it. **Correction 2 (NEGATIVE, `#guard`-backed)**: the deliverable carries NO Dyck FLOOR, even though the real forward lemma has a symmetric one — the consumer (`Located`/the FROM-LOCATED assembler) reads only `loS := p+1`, `loS ≤ a`, `bal d loS = bal d a` (`locatorAssembles` builds it in one line). On `[ ] [` (`dd = [1,-1,1]`, `bal dd 3 = 1`) the pruned existential is satisfied by BOTH the true innermost opener `p=2` AND a SPURIOUS outer opener `p=0` whose floor dips negative — `#guard floorOK dd 3 3 == true` vs `#guard floorOK dd 1 3 == false`; the floor would separate them, but the consumer needs neither innermost-ness nor the floor, only `bal d loS = bal d a`, so dropping the floor (≈ half the proof: the closer case's floor-extension across the skipped block) loses nothing the assembler consumes. The locator's deliverable shape is set by what the assembler READS ([[ref-from-located-assembler-direction]]: the assembler's hypotheses ARE the locator's exact contract), not by the forward lemma's symmetric conclusion-set. The de-risk was honestly pessimistic — the feared new infrastructure never materialized — so authoring is the real test of a de-risk's hardness prediction, sharpening [[ref-probe-deferred-universal-before-producing]])
* `FromLocatedAssemblerDirection`           — Reflection 308 (after the root seed lands, FACTOR the descent before authoring it: a FROM-LOCATED assembler discharges the existential trivially and makes the root a corollary; the de-risk then reveals the locator's DIRECTION is the residual's true shape — the toy of `seqEnclosingFacts_provider_of_located` (and `seqEnclosingFacts_provider_root` rewritten as its corollary), the producer-side parametric-assembler split of the `(i'-b-locator)` descent. A universal producer `∀ window, Gate → Deliverable`, already split into a ROOT seed + a DESCENT (R307), is factored ONE MORE TIME at the PRODUCER side: lift the locator's eventual output — a located enclosing `e` + its deliverable witness — as hypotheses into `assemble (w e) (h_seq) (h_reseat) : Deliverable w := ⟨e, h_seq, h_reseat⟩`, ONE line. The ROOT seed becomes a COROLLARY — `rootSeed w h := assemble w rootWin rootInfra h` ([[ref-parametric-assembler-extraction]] on the producer, [[ref-probe-provider-satisfiable-before-assembler]] for non-vacuity). Toy `Win` = `depth` + `seqTyped` (the gate, true at any depth); `Deliverable w := ∃ e, e.seqTyped ∧ w.depth = e.depth`. POSITIVE: `assemble nestedWin nestedEncl rfl rfl : Deliverable ⟨2,true⟩` (the located enclosing exists at the nested window — the assembler is satisfiable). NEGATIVE (1): `root_wrong_for_nested : ¬ (nestedWin.depth = rootWin.depth)` — the root witness is the WRONG enclosing for a nested window, so the descent is genuine. NEGATIVE (2) — the locator's DIRECTION: the producer NEEDS `locateEnclosing` (BOTTOM-UP, inner→outer: an interior window → its enclosing BODY, same depth) but the existing recursion provides `descendChild` (TOP-DOWN, outer→inner, strictly deepening); `#guard descendChild innerBody == none` ∧ `#guard locateEnclosing innerBody != none` — the top-down operation returns `none` exactly where the producer must return a witness, so the descent's residual is NEW inner→outer infrastructure (a backward last-crossing scan), not a reuse of the outer→inner descend oracle. Builds on [[ref-root-seed-discriminator-not-from-gate]] + [[ref-parametric-assembler-extraction]] + [[ref-probe-provider-satisfiable-before-assembler]]; the direction asymmetry echoes [[ref-near-leaf-mirror-sheds-machinery]])
* `RootSeedDiscriminatorNotFromGate`        — Reflection 307 (the ROOT seed of a universal-over-gated-windows producer carries a DISCRIMINATOR the gate cannot supply — the toy of `seqEnclosingFacts_provider_root`, the `(i'-b-locator)` base case. A universal producer `∀ window, Gate window → Deliverable window` is split into a ROOT seed + a DESCENT (per [[ref-universal-producer-root-seed-first]]); at the root the deliverable is infra-supplied. The non-obvious part: the root seed does NOT consume the universal's `Gate` — the gate is satisfied by seq-typed windows at ANY depth, including the descent's own targets, so it cannot tell a root-level window from a nested one. The root seed instead carries the top-level discriminator `AtRoot` (toy `flowBracketBalance tokens 2 a = 0`), strictly STRONGER than `Gate` and NOT derivable from it — correct, because it is the SAME quantity the descent re-establishes per level. Toy `Win` carries `bal` (balance from the outer origin; `0` ⇒ root level) + `seqTyped` (the gate); `Deliverable w := ∃ encl, encl.seqTyped ∧ w.bal = encl.bal` (an enclosing seq window at whose level `w` sits — the re-seated discriminator). `descentStep` takes the LOCATED enclosing as a hypothesis (in reality `recseqentry_seqbracket_oracle`); `rootSeed := descentStep … outerWin … hr` makes the shared discriminator literal — the root seed IS the descent step specialised to the outer window, `AtRoot` plugged into the re-seat slot. POSITIVE: `rootSeed rootWin atRoot_root` on `⟨0,true⟩`; `#guard rootWin.bal == 0`. NEGATIVE (the gate does not supply the discriminator): `gate_nested : Gate ⟨2,true⟩` holds yet `not_atRoot_nested : ¬ AtRoot ⟨2,true⟩`, and `outer_wrong_for_nested : ¬ (nestedWin.bal = outerWin.bal)` — the outer window is the WRONG enclosing for a nested window, so the root seed is inapplicable and the nested case needs the descent's located enclosing (`descentStep nestedWin ⟨2,true⟩ rfl rfl`). Contrast [[ref-guarded-universal-fold-relocates-guard]] (there the guard is the producer's GIFT narrowing its domain; here the discriminator is the seed's DEBT, a hypothesis it cannot discharge locally))
* `CoerceToWeakerReuseWrapper`              — Reflection 306 (reuse the WEAKER structure's existing wrapper via a strong→weak COERCION, not a mirrored wrapper — the toy of `SafeBodyUnit_safeBody` + `seqInteriorFeContentStart_of_windowed_safebodyunit`, the `(i'-b-encfacts)` move. A needed fact has the shape of an EXISTING wrapper proven on a WEAKER structure (`SafeBody`/`BodyW`), but the substrate in hand is the STRONGER `SafeBodyUnit`/`BodyS` the producer naturally delivers; the better move is COERCE strong→weak and reuse the wrapper, not MIRROR the wrapper onto the stronger type. Cheap because the strong refinement is *more of the same constraint* (`EntS`/`EntryUnit`'s `≥ 1` for ALL proper prefixes subsumes `EntW`/`EntrySafe`'s `≥ 1` only at separators) everywhere except the head, and the inductive's own `Q`-head field (with `hQ : Q v → ¬ sep`) closes that one gap. Toy substrate reuses the R304 `Tok`/`psum`/`isSep`. PER-ENTRY `entS_entW` (the coercion's half, only the `i = 0` head obligation is non-vacuous, closed by `hQ`); STRUCTURAL `bodyS_bodyW` (toy `SafeBodyUnit_safeBody`, two-constructor induction reusing `entS_entW`); WRAPPER `bodyW_headQ` proven ONCE on `BodyW`; PAYOFF `bodyS_headQ := bodyW_headQ ∘ bodyS_bodyW` — the fact for the strong body by reuse, not re-proof. POSITIVE: `bodyS_good` on `[con,sep,con]` + the reused wrapper `example`. NEGATIVE (the head gap is real): `entS_sep : EntS [sep]` holds (a lone separator IS a unit entry) yet `not_entW_sep : ¬ EntW [sep]` (its head is a depth-`0` separator) — so without `hQ` the coercion is FALSE, `hQ` load-bearing. The dual of [[ref-reduction-by-import]]: there wire an unconsumed consumer to retype a residual; here weaken a substrate to consume an existing producer-lemma)
* `ProbeProviderSatisfiableBeforeAssembler` — Reflection 305 (factor a heavy locate boundary as an EXISTENTIAL `provider`, discharge the ASSEMBLE via the proven rebase, and PROBE the provider is satisfiable before committing — the toy of `seqInteriorSeparators_of_enclosing_provider`, the `(i'-b-locate-enclosing)` factoring. The R304 rebase is the CONSUME side; the PRODUCE side — *locate* the enclosing window + *supply* its fact — is heavy, so it is lifted into a `∀ window, gate → ∃ enclosing, preconditions ∧ encFact` hypothesis. KERNEL `carrier_of_provider`: the carrier reduces to that provider by one `obtain` + the imported `succ_rebase` — the assemble is trivial, the locate isolated as the residual. The NEW lesson over plain [[ref-parametric-assembler-extraction]]: the lifted hypothesis is an EXISTENTIAL the locator must later satisfy, so PROBE it is *satisfiable* on a concrete witness before committing — a perfectly-proven assembler over an UNSATISFIABLE provider is a vacuous dead-end. Reuses the R304 toy substrate (`bal`/`Succ`/`succ_rebase`/`seqT`/`mapT`/`succB`/`not_succ_map`). POSITIVE: `ProviderAt seqT 1 4` (the enclosing IS the outer seq `[1,4)`, `loS = lo`, so the rebase precondition `bal loS a = 0` is exactly the gate's re-seating conjunct `hg.2`, the enclosing fact `succ_seqT`); the assembler yields `CarrierAt seqT 1 4` and a concrete `Succ seqT 3 4`; the satisfiability probe `#guard succB seqT 1 4 = true`. NEGATIVE (the lesson): on the map body `{ con : con }` the gated window `[1,4)` has a FALSE enclosing `Succ` (`#guard succB mapT 1 4 = false`), so `provider_mapT_unsat : ¬ ProviderAt mapT 1 4` — any provider would, through the CORRECT assembler, derive the false `Succ mapT 1 4`. The positive complement of [[ref-probe-provider-head-blind-gate]]: R303 probes a provider universal for FALSITY to ABANDON a route; R305 probes an extracted provider for SATISFIABILITY to VALIDATE the residual hypothesis the assembler stands on)
* `RebaseFactFromEnclosingWindow`           — Reflection 304 (a window-relative carrier fact RE-BASES from its ENCLOSING window's same fact by balance-`0` composition; the rebase precondition is load-bearing — the toy of `bodySuccFact_rebase`/`noTrailingSepFact_rebase`, the R303 redirect's first brick. R303 killed the per-gated-window `SafeBodyUnit` provider; the redirect: the carrier's separator facts at an arbitrary gated sub-window `[a,b)` instead re-base from the ENCLOSING seq interior `[loS,hiS)`'s own facts, no `SafeBodyUnit`. A `#guard`-backed probe confirmed the facts hold at EVERY gated window of `[[1,2],9]` (incl. the spurious separator-headed ones) and — on `[{a: 1}, 2]` — that the gate's enclosing-seq conjunct excludes EXACTLY the `bodySucc`-failure windows (the nested-mapping keys, where a key is followed by `:`/`val` not a separator). Toy substrate: tokens `opn`/`cls`/`sep`/`con`/`val`, `bal` a telescoping prefix-sum difference so composition `bal_compose` is UNCONDITIONAL and free, `Succ` the toy `bodySuccFact`. KERNEL `succ_rebase`: from the enclosing `Succ T loS hiS` + `loS ≤ a` + `b ≤ hiS` + `bal T loS a = 0`, derive `Succ T a b` — pure compose + `omega`, the enclosing close disjunct `k+1 = hiS` collapsing to `k+1 = b` because `k < b ≤ hiS`. POSITIVE: the seq body `[ con , con ]` — `succB seqT 1 4 = true` (enclosing), `succB seqT 3 4 = true` (rebased sub-window), `bal seqT 1 3 = 0` (start at top level); usability `example` feeds an enclosing `Succ` to `succ_rebase`. NEGATIVE (load-bearing): the map body `{ con : con }` — `succB mapT 1 4 = false` (the key at `1` is followed by `val`, not a separator) and `bal mapT 0 1 = 1 ≠ 0` (the failing window's start is NOT at the enclosing top level, so the rebase precondition correctly does not hold), capstone `not_succ_map : ¬ Succ mapT 1 4`. The lesson: when a carrier fact must hold at every gated sub-window but the per-window deliverable route is false, derive the fact by RE-BASING the enclosing window's same fact across the depth-`0` re-seating — composition + the gate's enclosing-type conjunct, which is load-bearing precisely because it supplies the `bal loS a = 0` precondition that excludes the failure windows)
* `ProbeDeferredProviderHeadBlindGate`      — Reflection 303 (probing a deferred `provider` universal BEFORE producing it catches a FALSE universal: a HEAD-BLIND gate admits spurious windows where the body-structure route is undischargeable — the toy of the de-risk that re-scoped `(i'-b-descend-root-provider-descent)`, behind `SafeBodyUnit_head_Q` / `not_safeBodyUnit_of_head_flowEntry`. The planned descent would prove the seq carrier via a `provider` universal delivering a `SafeBodyUnit` at EVERY window the gate `SeqTypedInterior` admits; but the gate (balance-`0` ∧ enclosing-seq) is HEAD-BLIND — it never inspects whether the window's first token is a content start — so it ADMITS separator-headed windows, where a `SafeBodyUnit` is impossible (its head must be a content start). The real minimal pair: on `scanFiltered (emit [[1,2],9])` = `SS [ [ "1" , "2" ] , "9" ] SE`, the two depth-`0` commas at indices `4`, `7` BOTH satisfy the gate at `[a,a+1)` yet their `[comma]` slice is no `SafeBodyUnit` — so `provider 4 5` / `provider 7 8` are unprovable. Toy substrate: tokens are `Nat`s (`0` = separator, `n>0` = content), `Gate l := l ≠ [] ∧ (l.map delta).sum = 0` (head-blind — every token is delta-`0`, mirroring `flowEntry`/scalar both `flowBracketDelta = 0`), `Body` = nonempty content-headed entries separated by single `0`s (toy `SafeBodyUnit`). KERNEL `Body_head_content` (a `Body` forces a positive head, toy of `SafeBodyUnit_head_Q`) and `not_Body_of_head_zero` (the dead-end guard). MINIMAL PAIR: `gate_good ∧ body_good` on `[1,0,2]` (content-headed: gate AND body) vs `gate_bad ∧ not_body_bad` on `[0]` (lone separator: gate STILL holds head-blind, but NO body). NECESSARY-BUT-NOT-SUFFICIENT `gate_trailing ∧ not_body_trailing` on `[1,0]` — a CONTENT-headed window can still fail `Body` (trailing separator), so content-start-alignment alone would not rescue the provider. CAPSTONE `provider_is_false : ¬ (∀ l, Gate l → Body l)` (witnessed at `[0]`) vs `good_window_discharges` (the carrier is true where the recursion actually instantiates it). Decidable `#guard`s: the gate accepts `[0]` and `[1,0,2]` alike (head-blind), the head differs (`1` vs `0`) yet `[1,0]`'s content head still fails `Body`. The lesson: when a deferred universal will be produced over a gate, `#eval`-probe a minimal pair FIRST — a head-blind gate that admits a window the deliverable cannot inhabit makes the producer's hypothesis FALSE; catching it before producing converts a planned multi-brick recursion into a retargeted direct discharge, and the false intermediate is fenced off by a proven lemma rather than a discovered sorry)
* `RootSeedRecursiveProducerSwap`           — Reflection 302 (the ROOT SEED of a recursive-deliverable producer reuses the FLAT derivation's slice/decomposition verbatim and swaps only the body producer; the slice bridge is PRODUCER-AGNOSTIC — the toy of `seqRoot_safeBodyUnit`. A `wrap body := 0 :: (body ++ [99])` container framing and its `windowSlice l := (l.take (l.length-1)).drop 1`; KEY `windowSlice_wrap : windowSlice (wrap body) = body` is about the CONTAINER SHAPE, never any property of `body` — so the ROOT SEED `rootSeed (P) (h : P body) : P (windowSlice (wrap body))` is generic over the producer `P` (mirror of `h_take_eq` transporting any body fact). `Flat` = all-positive (toy `WellTyped`), `Rec` = the structural all-positive chain (toy `RecSeqBody`), `Rec.toFlat` the projection. `flatRoot`/`recRoot` are the two instances — swap only `P`; `recRoot_reprojects` (nothing lost — `Rec.toFlat` re-projects the flat fact). KEY agnosticism witnesses `slice_agnostic_offFlat : windowSlice (wrap [0,5]) = [0,5]` + `flat_fails_offFlat : ¬ Flat [0,5]` (the slice holds for a body where `Flat` FAILS — it is about the framing, not the content). The lesson: to land the root seed of a recursive-deliverable producer, find the existing flat-fact derivation, observe its slice identity is producer-agnostic, re-run it swapping the recursive body producer for the flat one, project through the unchanged slice — one chain-replay you already have a template for, not a new induction)
* `GuardedUniversalFoldRelocatesGuard`      — Reflection 301 (the consumer fold collapses a GUARDED universal carrier to a single provider, and the guard RELOCATES — producer's gift, consumer's debt — the toy of `seqInteriorSeparators_of_safebody_provider`, the first brick of `(i'-b-descend-root)`. A carrier `Carrier lo hi := ∀ n, lo ≤ n → n ≤ hi → Gate n → Fact n` is a GUARDED universal; the per-element discharge `factOfUnit : Unit n → Fact n` (the R299 analog) folds under the `∀` into `carrier_of_provider`, whose sole hypothesis `provider` (deliver `Unit` at each gated `n`) IS the producer's remaining contract. The decisive point: the gate `Gate n` is the body's ANTECEDENT, so proving the carrier `intro`s it — the producer CONSUMES the gate, never produces it. Toy substrate `Gate` = even (the "seq-typed" context gate), `Unit = ∃ m, n = m + m` (the deliverable, "SafeBodyUnit"), `Fact = ∃ m, n = 2 * m` (the "separator fact"). POSITIVE `carrier_of_provider` (faithful mirror, `intro`s the gate, feeds the provided `Unit` through `factOfUnit`); `provider_concrete`/`carrier_concrete` (the root-instance analog — emission delivers `Unit` at every even `n ≤ 6`). KEY WITNESS `gate_gifts_narrower_domain`: `3 ∈ [0,6]` with `¬ Fact 3` AND `¬ Gate 3`, yet `Carrier 0 6` holds — the off-gate failure is GATED OUT, never reaching the provider (mirrors the R297 map-window where `bodySucc` FAILS but the seq gate excludes it) — the gate is the producer's GIFT (a narrower domain). DUAL WITNESS `consume_needs_gate`: instantiating `Carrier 0 6` at `4` requires SUPPLYING `Gate 4` — the gate is the consumer's DEBT, and this is where R300's `seqTypedInterior_of_opener` lives: at the consume/`assemble` site that instantiates the carrier, NOT in the root seed that proves it. Decidable `#guard`s: on-gate `4`/`6` even, off-gate `3` odd (gated out), `Unit` at `4` (`4 = 2 + 2`). The lesson: for a guarded universal `∀ x, G x → P x`, folding its consumer chain into a provider lemma `(∀ x, G x → Q x) → carrier` is the FIRST producer brick — it pins the contract AND relocates the guard `G` to its true home, the consume site; the guard is never the producer's burden, it is the producer's gift and the consumer's debt)
* `PrefixGateReconstructedFromBoundary`     — Reflection 300 (a PREFIX-quantified gate is NEVER projectable from an interior-only window guard — the toy of `enclosingMark_true_of_opener` / `seqTypedInterior_of_opener`, the Q2 discharge for `(i'-b-descend-root)`. The carrier's gate `SeqTypedInterior`'s `btFold`-stack-top conjunct quantifies over the window's PREFIX `[0,a)`, not its interior `[a,b)` — so `FlowBodyWindow` (whose `wellTyped` field only sees the interior) cannot project it. But it RECONSTRUCTS in place from two boundary facts the recursion already supplies: the opener at `q = a-1` is a `.flowSequenceStart` (push-`true`), and the pre-opener prefix folds to *some* typed stack. A `seqOpen` pushes `true`, so the stack top after the opener is `true`. Toy substrate `Tok` (`seqOpen` pushes `true`, `mapOpen` pushes `false`, `other` inert), `tfold`, and `enclosingMark l q := (tfold (some []) (l.take (q+1))).bind (·.head?)`. POSITIVE `enclosingMark_true_of_opener` (faithful mirror, same `take_add_one` split + single push + `head?_cons`): from a `seqOpen` opener + prefix folding to `some s`, the mark is `some true`. `enclosingMark_false_of_mapOpener`: the same shape with `mapOpen` yields `some false` — the discriminator (R297's minimal pair) is real. KEY NEGATIVE `opener_determines_mark_not_interior`: two lists with IDENTICAL interior (`drop 1`) but opposite openers have opposite marks, so the gate is provably NOT a function of the interior — it must be reconstructed from the boundary, not projected. Decidable `#guard`s: `seqOpen`-fronted ⇒ `some true`, `mapOpen`-fronted ⇒ `some false`, same interior either way, a nested `[` after a `[` still read off correctly. The lesson: when a consume-time gate quantifies over a window's PREFIX or boundary rather than its interior, do not look to project it from the interior-shaped guard — reconstruct it from the boundary token(s) + a global well-formedness fact restricted to the prefix; the reconstruction lemma's hypotheses ARE the per-step thread list the producing induction owes — proving the discharge both retires the de-risk and itemizes the producer's contract)
* `BoundaryResidualEndDual`                 — Reflection 299 (the BOUNDARY residual is the INTERIOR fact's END-dual, discharged VACUOUSLY — the toy of `SafeBodyUnit_last_not_sep` / `SafeBodyUnit_array_last_not_sep_window` and the per-window discharge `seqSeparatorFacts_of_windowed_safebodyunit`. A producer-guard residual splits into an INTERIOR fact (every separator is followed by content) and a BOUNDARY fact (the body never ENDS in a separator). The boundary fact is the END-dual of the interior one over the SAME inductive substrate, and it discharges VACUOUSLY: the conclusion is unprovable directly — it asserts something about the token PAST the window, which the window's substrate cannot see — but it is reached only via a premise "the last in-window token is a separator", which the boundary lemma REFUTES, so the conclusion follows by `absurd`. Toy substrate `UBody`: positive "unit" tokens (content) separated by single `0`s (the separator, like `.flowEntry`). POSITIVE `UBody_ends_pos`: the structural END-dual — every `UBody` is `pre ++ [n]` with `0 < n`. POSITIVE `UBody_last_not_sep` (the faithful mirror of `SafeBodyUnit_last_not_sep`, same `getElem?_append_right` index bridge): the last token (`k + 1 = length`) is `≠ 0`. POSITIVE `UBody_noTrailing`: assuming the last token IS the separator yields `False` — the vacuous discharge, the toy of `noTrailingSepFact` via `absurd`. Decidable model: `ubodyB`/`endsInSepB`; `#guard ubodyB [3,0,5] ∧ !endsInSepB [3,0,5]` (well-formed, no trailing separator) and NEGATIVE `#guard !ubodyB [3,0,5,0] ∧ endsInSepB [3,0,5,0]` plus `!ubodyB [3,0]` — a body ending in a separator is NOT well-formed, so the boundary the `noTrailing` premise speaks of is never reached (`trailing_sep_rejected`). The lesson: when a producer-guard residual splits into an interior and a boundary fact, suspect the boundary is the interior's END-dual over the same substrate, discharging vacuously by refuting that the boundary is a separator — prove the dual rather than survey derivability; the proof answers the question, lands the brick, and reveals both facts flow from one deliverable (`seqSeparatorFacts_of_windowed_safebodyunit`: one windowed `SafeBodyUnit` ⇒ both carrier facts))
* `WindowAbsoluteGateSubsetRestriction`     — Reflection 298 (a WINDOW-ABSOLUTE gate makes the carrier a SUBSET restriction — the toy of `SeqInteriorSeparators` and its three-line `_narrow`/`_descend`/`_advance` edges. When a guard conjunct quantifies over sub-windows `[a,b) ⊆ [lo,hi)` under a gate, push the outer origin OUT of the quantifier body into the domain bounds: a body keyed only on `a,b` (never `lo`/`hi`) restricts to any sub-interval verbatim, so ONE monotonicity lemma is the whole descend/advance edge; a body that re-bases a fact on the origin BREAKS under narrowing (the `ref-non-restriction-residual-root-seed` failure). Gated positions `gate a := a % 2 = 0`. POSITIVE `carrierAbs_narrow`: the abstract window-absolute carrier `∀ a b, lo ≤ a → a ≤ b → b ≤ hi → gate a → factAbs a` restricts to `[lo',hi')` by two `Nat.le_trans` — the entire edge. POSITIVE `abs_survives_narrowing`: `carrierAbsB 0 4 = true ∧ carrierAbsB 1 4 = true` (the window-absolute fact `a % 2 = 0` survives moving the origin `0 → 1`). NEGATIVE `rel_breaks_under_narrowing`: the origin-relative fact `factRel lo a := (a - lo) % 2 = 0` gives `carrierRelB 0 4 = true` (offsets `{0,2,4}` of the gated even positions are even) but `carrierRelB 1 4 = false` (the SAME gated positions `{2,4}` now have odd offsets `{1,3}` — `#guard (2-0)%2=0` vs `(2-1)%2=1` is the re-basing flip). The lesson: choosing where the window origin lives is the whole game — origin in the bounds ⇒ subset restriction for free; origin in the body ⇒ a per-edge descent proof that may be impossible. The remaining cost is the ROOT seed, which no body-design makes free)
* `MinimalPairExtractsTheGate`              — Reflection 297 (probe a gated `∀`-over-sub-windows as a MINIMAL PAIR; the gate is read off, not invented — the toy of the `BodySuccSeqDiscriminator` concrete-emitter probe. When a deferred universal is FALSE in general but you suspect a gate rescues it, do not probe a single (holding) instance — probe a minimal pair inside one object: one window that HOLDS the property and a sibling that FAILS it. The object `[ { k : v } , x ]` (`tok`/`stream`) holds a seq interior `[1,8)` and a map interior `[2,5)`. POSITIVE `seq_window_holds ∧ seq_window_is_seq_typed`: `succHolds 1 8 = true` (toy of `bodySucc` — the nested map is invisible, balance `> 0` there) and the gate classifies it `enclosingMark 0 = some true`. NEGATIVE `map_window_fails ∧ map_window_is_map_typed`: `succHolds 2 5 = false` (the key at `k=2` is a depth-`0` end followed by `colon`, not a `comma` separator) and the gate classifies it `enclosingMark 1 = some false`. The probe does triple duty: confirms the gate SUFFICIENT (holding window), proves it NECESSARY (failing sibling), and NAMES it as the existing typed-stack-top computation (`stFold`/`enclosingMark`, toy of `WellTyped`/`btFold`: `seqOpen ↦ true`, `mapOpen ↦ false`) that SEPARATES the pair. The lesson: the structural feature that differs between the holding and failing windows IS the discriminator — read it off the pair, do not invent it)
* `NonRestrictionResidualNeedsRootSeed`     — Reflection 296 (a non-restriction residual needs a ROOT SEED, not a stronger local invariant — the toy of `flowBodyContent_bodySucc_of_part6`, the certain root-seed half of (i'-b). The recursion's guards thread cleanly only because their fields are all-depth and balance-FREE, making each edge a pure SUBSET restriction; a depth-`0`-balance-keyed field (`bodySucc`) has no such form, so no LOCAL carrier descends. POSITIVE `afterOpener_restrict`: an all-depth `AfterOpener` (toy of `FlowBodyContentDeep`'s opener/separator fields) restricts to any sub-window by `omega` — the three-line descend/advance edges. NEGATIVE `parent_holds ∧ child_fails`: the depth-`0` `Depth0Sep` (toy of `bodySucc`) holds on the parent window `[0,4)` of `wt = opn scal scal cls` (`[ a b ]`, its only balanced-prefix end the close at `k=3`) yet FAILS on the descended interior `[1,3)` (there `bal 1 2 = 0` fires on the scalar at `k=1` — the child's depth-`0` = the parent's depth-`1`, which the parent never speaks of — not followed by a separator); same `tok`, parent holds, child fails, so it is NOT a subset restriction. POSITIVE `bridge_checked_to_bang`: the root-seed move — infra delivers the fact with bounds-checked indexing `T[k]'h`, the consumer wants panic `T[k]!`, the bridge is a pure `getElem!_pos` conversion. The lesson: when a needed fact is provably NOT a subset-restriction, do not chase a stronger local invariant — seed its ROOT instance from existing infra now, and recognize the descended instances as a bracket-TYPE-context problem)
* `IncompleteProjectionStillFactors`        — Reflection 295 (an INCOMPLETE guard-projection still factors — the toy of `flowBodyContent_of_deep`, the assembler that PRODUCES the head-dispatch's `FlowBodyContent` from the threaded deep guard. When a strengthened carrier projects MOST of a target structure but not all, build the ASSEMBLER that projects the covered part and NAMES the residual, not a heavier carrier. The projection is partial in TWO grains: `head` is a WHOLE-FIELD free projection (`DeepGuard.head` and `Content.head` are the SAME proposition), `sepNext` is a CASE-SPLIT projection — the carrier covers its INTERIOR (`k+1 < n`, the `sepNextInterior` field gated `k+1 < n`) but not its BOUNDARY (`k+1 = n`). POSITIVE: `assemble` builds `Content` from `DeepGuard` plus two named residual hypotheses — the `head` arm a free projection, the `sepNext` arm `rcases`-split into projected-interior / residual-boundary. NEGATIVE: `valSep_not_derivable` — the carrier is SEPARATOR-BLIND, `DeepGuard 2 twoScalars` holds (two adjacent scalars `[a b]`) yet `valSep` (comma-separation, toy of `bodySucc`) FAILS, so it is genuinely owed; `boundary_not_reachable` — `DeepGuard 2 trailingComma` holds (`[a ,]`) yet `sepNext` FAILS at the boundary `k+1 = n` (trailing comma, `tok 2 = cls` not content-start), so the interior-gated carrier field cannot reach it. The lesson: the residual you isolate — here one whole field (`valSep`) PLUS one missing CASE of another (`sepNext` boundary) — is precisely the data the carrier semantically cannot encode; name it, the names are the real owed primitives)
* `ThreadedGuardPrunesCaseTree`             — Reflection 294 (a threaded guard PRUNES the case tree — the toy of `recseqentry_window_dispatch`, the per-window head-shape dispatch the dependency map wrote as a FOUR-way split that is actually THREE. The dispatch reads the head off `isContentStart` (THREE shapes — scalar / `sopen` / `mopen`, never the closer); the deep guard's `OpenerContentStart` (after a `sopen`, the next token is content-start) makes the empty-bracket leaf `[ ]` unreachable, so it is EXCLUDED, not handled — folded into the nested-`[ … ]` branch. POSITIVE: `dispatch` is TOTAL over the three content-start heads (its `sclose` case vacuous, `isContentStart sclose = False` via `h.elim`); `empty_excluded` shows under the guard a `sopen`-headed window never has `sclose` next. NEGATIVE: `not_content_start_sclose : ¬ isContentStart sclose` (the closer is no head); `empty_reachable_without_guard` shows the empty bracket IS a genuine fourth shape absent the guard — so it is the GUARD, not the deliverable's type, that prunes the branch (the `Shape` type still carries `emptyBracket` distinct from `nestedSeq`). The lesson: a threaded recursion guard is not only a source of premises — a guard strengthened for one purpose can be strong enough to DELETE a branch the deliverable still anticipates, so the structural moves completing a recursion can be fewer than the constructor count)
* `NearLeafMirrorShedsMachinery`            — Reflection 293 (a near-leaf mirror SHEDS the recursion machinery its recursive sibling needed — the toy of `recseqentry_mapbracket_oracle`, the `{ … }` mirror of the recursive seq oracle. The R244 storage asymmetry has OPPOSITE signs on the two sides of the boundary: a near-leaf is the CONSUMER's complication but the PRODUCER's simplification, so mirroring a near-leaf oracle is a hypothesis-DROPPING exercise. `RBody` is the recursive deliverable (inhabited only by its constructors, UNINHABITED on `[]`), `WB l := pbal l = 0 ∧ ∀ i, pbal (l.take i) ≥ 0` the near-leaf (flat prefix-balance, inhabited on `[]`). POSITIVE: `seq_oracle` is discharged ONLY with the IH AND non-emptiness (both load-bearing); `map_oracle` produces the same-shape deliverable from balance facts ALONE — both hypotheses GONE — and `map_fires_empty : WB []` fires on the degenerate empty interior the recursive sibling cannot represent. NEGATIVE: `not_rbody_empty : ¬ RBody []` shows the empty case is a HOLE for the recursive sibling (so the seq oracle's non-emptiness guard is real), FREE for the near-leaf (`WB []` holds) — the storage asymmetry's two opposite signs on one input; `not_wb_opn : ¬ WB [opn]` confirms `WB` is not vacuous, the near-leaf oracle being IH-free, not content-free. The lesson: write the recursive sibling first (it forces the full machinery), then mirror the near-leaf by DELETING the IH and the descend/non-emptiness guard, not adding to them)
* `ProducerGuardedOracleReconstruct`        — Reflection 292 (discharge a producer-guarded oracle by RECONSTRUCTING the descended guard in place from the locator's own facts — never by re-running the *locating* descend-lemma, which finds its own witness `j'` and saddles you with a `j = j'` matching-close uniqueness proof; the toy of `recseqentry_seqbracket_oracle`. The oracle hands in the close `j` WITH the facts that pin it (`lo+1 < j`, `bal l (lo+1) j = 0`), so `win_reconstruct` builds the descended guard `Win l (lo+1) j` at THAT `j` directly — parametric in `j`, no re-location — and `oracle_discharge` feeds it to the IH (`reconstruct_fires : P nested 1 3` on the nested bracket `[opn,opn,cls,cls]`, the outer close `j = 3`). NEGATIVE: the naive re-locator `firstClose nested = 2` finds the INNER close, not the outer matching close `3` the oracle was handed (`#guard firstClose nested != 3`), and `bal nested 1 2 = 1 ≠ 0` shows the guard at that re-located witness targets the WRONG, unbalanced window — substituting the re-located witness is unsound without uniqueness, which reconstruct-in-place never incurs. SECOND FACET — the field the strengthened guard DROPPED resurfaces as a hypothesis: `Deep` (toy of `FlowBodyContentDeep`, balance only, the `succ`/`bodySucc` field dropped because its descend edge was unprovable) `deep_holds` on `noSep = [opn,cls,val]` yet `deep_lacks_succ` (the successor at `2` is `val`, not `sep`) — so the dispatch's trailing-separator successor is unrecoverable from `Deep` and must come from `Full` (toy of `FlowBodyContent`, carrying `succ`), `full_supplies_succ`. The lesson: locate ONCE in the consumer, RECONSTRUCT (don't re-locate) in the producer; and a guard's deliberately-shed field returns as the consumer's own owed hypothesis)
* `LiftOnceMirrorEasyEdge`                  — Reflection 291 (lift once, then MIRROR the easy edge — the dividend-collection sequel to [[ref-easy-edge-guard-fails-hard-edge]] / R290 and the resolution step of [[ref-converse-forward-invariant-asymmetry]]. R290 diagnosed that a guard sized to its easy (ADVANCE) edge can be structurally incapable of its hard (DESCEND) edge, and lifted the content guard to the all-depth, balance-FREE `Deep`; R291 is the dividend: once lifted, the easy edge is ALREADY PAID FOR. A balance-free guard is a pure RESTRICTION of itself on every sub-window, and that property is direction-agnostic, so the advance edge restricts the same all-depth quantifiers to the tail `[m+1, hi)` exactly as the descend edge restricts them to the interior `[k+1, j)` — the ONLY difference is which field re-seats the child head (OPENER at `k` for descend, SEPARATOR at `m` for advance). POSITIVE: `deep_descend` (toy of `flowBodyContentDeep_descend`) and `deep_advance` (toy of `flowBodyContentDeep_advance`) have the IDENTICAL proof skeleton (`obtain ⟨_, h_op, h_fe⟩; refine ⟨?_, ?_, ?_⟩`; head off one field; the two quantifier fields the parent's restricted by `omega`), differing only in the one head-seating line (`h_op k …` vs `h_fe m …`) — and `deep_advance` takes NO balance hypothesis and NO size side condition; `deep_descend_good` fires descend on `[ a a ]` → `[1,3)`, `deep_adv`/`deep_advance_good` fire advance on `a , a` → `[2,3)`, re-seating `hdAdv 2 = scal` with zero arithmetic (`#guard`ed). NEGATIVE: re-deriving advance from the depth-`0` PROJECTION `Shallow` costs the balance gate — on `[ , , ]` (`hdNeg = opn sep sep cls`, separators at depth `1`), `shallow_holds_neg` shows the shallow guard HOLDS vacuously (its gate `bal 0 k = 0` is false at the depth-`1` separators), yet `interior_after_sep_not_content` shows the post-separator head `hdNeg 2 = sep` is NOT content-start, so the shallow guard gives nothing toward advancing there; `not_deep_neg` confirms the deep guard's UN-gated separator field is exactly the repair (it rejects the stream). The lesson: after lifting a guard to carry its hard edge, do NOT re-derive the easy edge with the old projection-level machinery — write it as the hard edge's mirror, swapping only the head-seating field)
* `DescendStrengthInvariant`                — Reflection 290 (a guard sized to its easy (ADVANCE) edge can be structurally incapable of its hard (DESCEND) edge — the sequel to R289 and a fresh instance of [[ref-converse-forward-invariant-asymmetry]] fused with [[ref-probe-deferred-universal-before-producing]]. R289's content guard landed its ADVANCE edge cheaply, and that ease MASKED that the guard cannot carry the DESCEND edge: descending into a nested bracket `[k .. j]` must hand the interior head `hd (k+1)` as content-start, but that head sits at balance `1` while every field of the depth-`0` guard is gated by `bal lo k = 0` — no outer field even mentions the interior head. The edge is not hard to *prove*; the guard is too weak to *carry*. `Shallow f hd lo hi := isContentStart (hd lo) ∧ ∀ k, … hd k = .sep → bal f lo k = 0 → isContentStart (hd (k+1))` toys R289's `FlowBodyContent` (depth-`0`, no opener field); `Deep hd lo hi := isContentStart (hd lo) ∧ (∀ k, … tokDelta (hd k) = 1 → isContentStart (hd (k+1))) ∧ (∀ k, … hd k = .sep → isContentStart (hd (k+1)))` toys `FlowBodyContentDeep` — content at ALL depths, balance-FREE. POSITIVE: `deep_descend` (toy of `flowBodyContentDeep_descend`) transports `Deep` into the interior `[k+1, j)` as a pure RESTRICTION — child head off the parent's opener field at `k`, child fields the parent's quantifiers restricted, no re-basing/no bracket arithmetic; `deep_good`/`deep_descend_good` fire it on the nested `[ a a ]`, yielding the interior guard on `[1, 3)` with head `scal`. NEGATIVE: on the leading-separator interior `[ , a ]` (`hdBad = opn sep scal cls`), `shallow_holds` shows the shallow guard HOLDS (its only separator sits at balance `1`, so its depth-`0` gate `bal 0 1 = 0` is vacuous), yet `interior_head_not_contentstart` shows the interior head `hdBad 1 = sep` is NOT content-start — so the shallow guard, though true, gives nothing toward the interior guard; `not_deep` confirms the DEEP guard correctly REJECTS the same stream (its opener field at `0` demands exactly the interior head be content-start). The lesson: probe the hard edge's provability against the guard AS DEFINED — the remedy is a stronger invariant (lift to the descend edge's depth), not more proof effort)
* `AdditiveParallelGuard`                   — Reflection 289 (a too-weak shared guard's missing content is its own additive parallel guard — the guard-layer instance of [[ref-additive-parallel-type-over-shared-edit]]. The head-shape dispatch needs "head is content-start", and that is NOT derivable from the bracket guard `FlowBodyWindow` (balance/dyck/WellTyped): a separator-headed window is still balanced+dyck+WellTyped. `BracketWin f lo hi := bal f lo hi = 0 ∧ ∀ i, bal f lo i ≥ 0` toys the bracket guard; `dyck_excludes_closer` shows the dyck floor genuinely forbids a leading CLOSER (`tokDelta (hd lo) ≥ 0`), but `separator_head_passes_dyck` (`0 ≥ 0`) + `separator_head_not_contentstart` show a separator head clears the SAME floor while not being content-start, and `bracketwin_sep_head`/`sep_head_not_cs` exhibit a whole window passing the bracket guard whose head is a separator — so content-start is not a function of the guard. The fix: name the missing content as a parallel guard `ContentWin f hd lo hi := isContentStart (hd lo) ∧ ∀ k, … hd k = .sep → bal f lo k = 0 → isContentStart (hd (k+1))` (toy of `FlowBodyContent`'s `headContentStart` + `feContentStart`), defined as what the consumer already eats. POSITIVE: `content_advance` (toy of `flowBodyContent_advance`) transports `ContentWin` to the tail `[m+1, hi)` by pure re-basing — the tail head re-seated from `feContentStart` at the separator; `content_window_good`/`content_advance_good` fire it on `scal sep scal`, re-seating `hdGood 0 = scal` to `hdGood 2 = scal` across the separator at `1` (`#guard`ed). NEGATIVE: `not_contentwin_bad` — the empty-entry body `scal sep sep scal` is NOT a `ContentWin` (the separator at `1` is followed by another separator, not a content-start head), so `feContentStart` is the load-bearing field whose failure would break ADVANCE re-seating; the recursive DESCEND content edge — interior heads a nesting level down — is left as the isolated residual)
* `GuardEdgeFloorAsymmetry`                 — Reflection 288 (the DESCEND edge completes R287's guard-threading skeleton, and its lesson is that the SAME guard field needs DIFFERENT invariant strengths on different recursion edges — an instance of [[ref-converse-forward-invariant-asymmetry]] *within one recursion*. `bal f a b = f b − f a` toys `flowBracketBalance`; `advance_dyck` (toy of `flowBodyWindow_advance`'s `dyck`) derives the tail `dyck ≥ 0` from JUST the OUTER `dyck ≥ 0` because the tail re-bases onto a *balanced* prefix (`bal lo (m+1) = 0`); `descend_dyck` (toy of `flowBodyWindow_descend`'s `dyck`) needs the strictly-stronger FLOOR `bal lo i ≥ 1` because the interior sits a level DOWN (the opener pushes the balance to `1`, `bal (k+1) i = bal lo i − 1`) — the floor being what `flowBracketBalance_matching_close` threads through its forward scan, NOT recoverable from the guard's outer `dyck`. POSITIVE on a real matched bracket `fNest = "[ [ ] ]"`: `nest_offset`/`nest_floor`/`nest_interior_dyck` (floor holds ⇒ interior `dyck` follows). NEGATIVE on `fFlat = "[ ] [ ]"`: `flat_outer_dyck` (outer `dyck ≥ 0` holds everywhere) yet `flat_interior_dips` (`bal fFlat 1 2 = −1` dips below 0) and `not_flat_interior_dyck` (the descend conclusion is outright FALSE), because `flat_floor_fails` (`bal fFlat 0 2 = 0`, not `≥ 1`) — so the advance edge's weaker hypothesis cannot be reused on the descend edge; `#guard`s confirm both edges strictly narrow the width metric)
* `GuardThreadingSkeleton`                  — Reflection 287 (the per-window `step` has a *guard-threading skeleton* below its grammar core: Reflection 286's combinator left its guard `G` abstract, and driving it requires `G` made concrete and shown preserved across each recursion edge *before* the head-shape grammar can run — that layer is grammar-free and, because a well-chosen `G` names no collection-specific deliverable type, SHARED across both axes. `BodyWin` (toy of the real `FlowBodyWindow`) bundles frame bounds + non-emptiness `lo < hi` + balanced/Dyck + a content predicate `wt`, naming NO deliverable type; `bodyWin_advance` (toy of `flowBodyWindow_advance`, its proof the real one verbatim) transports the WHOLE guard from `[lo, hi)` to the tail `[m+1, hi)` given only the separator's three transport certificates — tail balanced, depths re-base to the outer origin (toy of `advanceTail_invariant`), content carries to the subrange (toy of `WellTyped_subrange`) — with the only non-trivial field, `Dyck`, closed by re-basing each tail prefix to `lo` then applying the outer `Dyck`; the non-emptiness premise `m+1 < hi` (no trailing separator) is DEFERRED to the caller (the head-shape grammar's burden). One guard, both axes: `feedSeq`/`feedMap` (stand-ins for producing a `RecSeqBody`/`RecMapBody`) are gated by the SAME `BodyWin`. Positives `win_2_6` (a concrete guard) and `win_4_6` (`bodyWin_advance` firing to the tail), `#guard feedSeq/feedMap win_2_6 == 4`; negatives `not_bodyWin_empty` (the `lo < hi` field is LOAD-BEARING — the R285 echo — so the guard EXCLUDES the empty window the deliverable cannot inhabit) and `#guard (6 - (3+1)) < (6 - 2)` (the ADVANCE edge strictly NARROWS the window, so the combinator's width metric decreases))
* `WidthRecursionCombinator`               — Reflection 286 (the single-named "strongRecOn width-metric driver" is TWO bricks entangled — the grammar-free recursion plumbing and the per-window grammar step; cut the plumbing off as an abstract width-metric combinator `widthRec` (over arbitrary `P`/`G`, proved by the codebase's span-bound `Nat.strongRecOn` idiom). It pins the per-window step's IH interface exactly (`hi' - lo' < hi - lo → G lo' hi' → P lo' hi'`) so the step is a *non-recursive* lemma carrying no well-founded-recursion risk, and (naming no collection-specific deliverable type) it does NOT re-split across seq/map — the SAME `widthRec` drives two structurally-different toy deliverables verbatim: `Reach` (descends from `lo`) and `Reach2` (descends from `hi`), each via a non-recursive per-window `reachStep`/`reach2Step` that REQUIRES the combinator's oracle (`reach_all`/`reach2_all`, `reach_0_3`/`reach2_0_3` positive). The guard is load-bearing (the R285 echo): `Reach lo hi` is inhabited only for `lo ≤ hi` (`reach_le`), so `G := (· ≤ ·)` keeps the recursion inside the inhabitable region — outside it the deliverable is uninhabited (`not_reach_3_0`), exactly the empty-window region R285 peeled away to make the driver's interface satisfiable; negatives also `#guard` that the per-window step's recursive call genuinely DECREASES the shared width metric `hi - lo` on both axes)
* `EntryBoundaryLocator`                    — Reflections 264, 265, 266, 267, 268 & 271 (the analytical entry-boundary location splits into an *input side* — locate the split point — and a *shape side* — classify the item; the input side is a single axis-agnostic combinatorial brick, because the body-separator token is shared across seq and map. A general constructive least-witness locator `exists_least_in_range` (for a decidable `P` holding at `start + gap`, the least witness with its minimality certificate; structural induction on the gap, decidability-driven upward scan — no `Nat.find`, no classical choice, no well-founded recursion — *verbatim* the real lemma) and its specialization `firstEntryBoundary`: for a balanced window (`bal l l.length = 0`) the least depth-`0` boundary marker `m` (balance returns to 0, and `m` is the window end or a separator) with the certificate that no earlier interior position is such a marker. This pins *where* the first item ends, separate from the shape side (the matching-close analysis that *consumes* `m`). **The discriminator**: a navigation brick mirrors across seq/map exactly when it names a collection-specific *deliverable type*; one phrased over the shared *token stream* (balance, the separator `FE`) is written once — the input side does NOT mirror, the shape side will. Input-side positives `firstMarker_l1` (the locator fires on the balanced `[a],b`, the first marker being the separator at `m = 3`, witnessed concretely by `#guard bal l1 3 == 0`/`isFE (tokAt l1 3)`, minimality by `bal l1 1 == 1`/`bal l1 2 == 1`) and `firstMarker_l2` (the *last-item* branch — the window end `m = 3 = length` on the single item `[a]`, no separator); `least_ge3` exercises the helper on a pure-`Nat` predicate; input-side negatives `not_marker_inside` (an interior depth-≥1 position is not a marker) and `cl_is_not_separator` (the boundary token is `FE`, not the close bracket). **Reflection 265 (shape side)**: the shape side is the *family of per-constructor window-lifts* on the entry inductive `Entry` (`scalar` leaf / `seq` recursive), and the recursive constructor's lift is the BUILD structural move (already landed), so the new work is the non-recursive leaf — `entry_scalar_window` (toy of `recseqentry_scalar_window`): the one-token window at a scalar head is an `Entry.scalar` (window-singleton identity via `List.getElem_cons_drop`/`List.getElem_take`/`List.drop_eq_nil_of_le`, head value off `tokAt` via `List.getElem_eq_getD`). Shape-side positives `scalar_window_l1_1`/`scalar_window_l1_4` (the leaf fires at each scalar head of `l1`, witnessed by `#guard (l1.take 2).drop 1 == [sc]`); shape-side negatives `op_head_is_not_scalar_leaf` (an `op` head is the recursive constructor's job, not the leaf's) and `#guard (l1.take 3).length == 3` (the bracketed first item is not the one-token leaf shape). This is where the seq/map mirror re-splits — the lemma names the entry inductive, so the map shape side's leaf is a heavier key/value *pair*. **Reflection 266 (the leaf *family*)**: the non-recursive leaves come as a family — `entry_seqempty_window` (toy of `recseqentry_seqempty_window`) is the scalar leaf *scaled by one token*: the empty bracket `[ ]` at an `op`-then-`cl` head is an `Entry.seqEmpty` over the two-token window `(l.take (lo+2)).drop lo` (`m = lo + 2`, still non-recursive), proven by the same `List.getElem_cons_drop` peel run twice (`[lo]` then `[lo+1]`) with the same trailing-`drop`-nil and `getElem_take`/`getElem_eq_getD` — only the arity moved; once the first leaf is proven the rest are the same proof at a different arity. Positive `seqempty_window_l3_0` (the leaf fires at the head of `l3 = [op, cl, fe, sc]`, witnessed by `#guard (l3.take 2).drop 0 == [op, cl]`); negatives `#guard (l3.take 2).drop 0 != [op]` (two-token shape ≠ the scalar leaf's one-token shape — distinct family members) and `l1_head_is_not_empty_bracket` (the empty leaf needs `cl` immediately after `op`; in `l1` the token after the opener is `sc`, the recursive `seq` constructor's job). **Reflection 267 (the family's last member — the near-leaf)**: `entry_map_window` (toy of `recseqentry_map_window`) is the nested-mapping near-leaf — the recursive `seq` BUILD move *minus one field*. A distinct map-bracket pair `mo`/`mc` (toy of `.flowMappingStart`/`.flowMappingEnd`) and a flat well-bracketed predicate `WB l := bal l l.length = 0` model the storage decision: the `Entry.map` constructor STORES only `WB interior` (not a recursive body, R244), so it is a *near*-leaf (variable-width interior) that still terminates the dispatch. Its proof transports the recursive `seq` build's window plumbing verbatim — rest-decomposition `(take (hi+1)).drop (lo+1) = (take hi).drop (lo+1) ++ [l[hi]]` (`List.take_add_one`/`List.drop_append_of_le_length`), opener peel (`List.getElem_cons_drop`/`List.getElem_take`) into `op :: (interior ++ [cl])` shape, head values off `tokAt` — differing only in the terminal constructor (`Entry.map`) and the one bare `WB` field (the one-field arity delta *is* the store-vs-project decision). Positive `map_window_l4_0` (the near-leaf fires at the head of `l4 = [mo, sc, mc]`, witnessed by `#guard (l4.take 3).drop 0 == [mo, sc, mc]` and `interior_l4_is_wb : WB ((l4.take 2).drop 1)`); negatives `#guard (l4.take 3).drop 0 != [mo]` (≥2-token shape, not the scalar leaf's one-token), `unbalanced_interior_not_wb` (the stored `WB` fails for the unbalanced `[mo]` — the flat fact stored in place of a body), and `cl_is_not_map_close` (the near-leaf needs the *map* closer `mc`, distinct from the sequence `cl`). With scalar + seqEmpty + map + the BUILD-move `seq`, the seq shape side's four-way head dispatch is complete. **Reflection 268 (the map shape side — one assembler, not a dispatch)**: the shape side re-splits across seq/map (it names the collection-specific `Pair` inductive), but the map side is *not* a per-constructor dispatch — a map item is always a whole key/value *pair* `ky <block_k> vl <block_v>` (`Pair`, sole constructor `Pair.mk`), so there is nothing to classify. `pair_window` (toy of `recmappair_window`) is ONE assembler that (i) *consumes the complete seq dispatch as its two sub-blocks* — the key/value blocks are arbitrary `Entry`s, taken as hypotheses, so the map side adds zero per-constructor classification — and (ii) supplies the one new piece, the pair glue, as a *composition of two already-landed plumbing patterns*: the segment split at the `vl` separator (the ADVANCE plumbing `List.take_append_drop`/`take_take`/`drop_drop`, splitting `[lo+1, m)` at `kv`) and the opener peel of `l[lo]` (the BUILD/leaf peel `List.getElem_cons_drop`/`List.getElem_take`), terminated by `Pair.mk`. New tokens `ky`/`vl` (toy of `.key`/`.value`, delta 0). Positive `pair_window_l5` (the assembler glues two scalar `Entry` blocks — both produced by the *seq* scalar leaf — into a `Pair` of `l5 = [ky, sc, vl, sc]`, witnessed by `#guard (l5.take 4).drop 0 == [ky, sc, vl, sc]` and the sub-blocks `#guard (l5.take 2).drop 1 == [sc]`/`#guard (l5.take 4).drop 3 == [sc]`); negatives `#guard (l5.take 4).drop 0 != [sc]`/`!= [ky]` (the pair is a heavier four-token composite, not a single `Entry`), `seq_head_is_not_key` (an `op` sequence head is not a `ky` map head), and `fe_is_not_value` (the seq separator `fe` is distinct from the pair's `vl` marker). So the seq/map re-split surfaces as *composition* — ADVANCE-split ∘ BUILD-peel — not a fresh family. **Reflection 271 (the ADVANCE-step *invariant-preservation* certificate — structural-complete ≠ runnable)**: the moves + locator + shape side make the recursion structurally complete, but it is not *runnable* until one more fact is proved — after ADVANCEing past a depth-`0` separator at `m`, the tail `[m+1, hi)` is *itself a valid recursive sub-instance*. The structural moves are all *assembly* lemmas (given the pieces, build the constructor); none certifies the recursive call's *precondition*. `advanceTail_invariant` (toy of the real `advanceTail_invariant`) is that certificate — and like `firstEntryBoundary` it is axis-agnostic (names no deliverable type), so it sharpens R264's input/shape split: the input side is *locate the point* **and** *certify the tail*, both written once. It needs a *range* balance `balR l lo hi` (toy of `flowBracketBalance`), its additivity `balR_compose` (the slice split `take (hi-lo) (drop lo) = take (mid-lo) (drop lo) ++ take (hi-mid) (drop mid)` via `List.take_add`/`List.drop_drop` fed to `sumD_append`/`foldl_add_shift`) and the single-token value `balR_single`; then, given a balanced window with a depth-`0` `fe` at `m`, it delivers (a) prefix-through-separator balanced, (b) tail balanced, (c) the tail *re-bases* (`balR lo p = balR (m+1) p` for `m+1 ≤ p ≤ hi`) — all by `balR_compose` split at `m`/`m+1` plus the separator's zero delta, no induction. Positive `advanceTail_l6` (the invariant fires on `l6 = [op, sc, cl, fe, op, sc, cl]`, the separator at depth `0` position `3` splitting `[a],[b]`; re-basing exercised *mid-bracket* by `#guard balR l6 0 5 == balR l6 4 5` — both `1` inside the tail's `[b]`, the outer origin `0` and new origin `4` agreeing precisely because the prefix is balanced — and `#guard balR l6 0 4 == 0`/`balR l6 4 7 == 0` for the two balance facts); negatives `interior_sep_not_depth0` (an `fe` *inside* a bracket, `l7 = [op, fe, cl]`, has `balR l7 0 1 = 1 ≠ 0`, so the depth-`0` hypothesis correctly excludes it — a separator token at depth ≥ 1 is not a body boundary) and `#guard balR l6 0 2 != balR l6 4 2` (re-basing is not a free identity — *before* the new origin the two frames disagree, it holds on the tail only because the prefix is balanced))
-/
